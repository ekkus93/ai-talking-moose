# AI Talking Moose — Local LLM Post-Review Remediation TODO

**Companion specification:** `docs/SPEC(20260905-141500).md`  
**Baseline:** `bb85beb4b61c6a25d7b5935ce4f6918b65afe8d6`  
**Predecessor tracker:** `docs/TODO(20260831-081800).md`  
**Status:** Complete — final remediation gate reclosed 2026-09-09  
**Scope:** Fix every issue identified by the 2026-09-05 Local LLM source review while preserving the successful Local LLM V1 architecture and historical evidence.

Task IDs use the `LLMR-###` prefix (**Local LLM Remediation**).

The previous Local LLM Final Gate is **narrowly reopened by post-review findings**. This does not erase the earlier passing evidence. It means later review found additional defects that require new remediation and reclosure evidence.

The signed/notarized P13 `v0.1.0` release-candidate workflow, physical Mac audio/TCC acceptance, and human voice audition remain **deferred by owner decision** and are not blockers for this tracker.

---

# P0 — Baseline, Reopening, and Regression Inventory

## LLMR-001 — Freeze the remediation baseline and boundaries

- [x] Record `bb85beb4b61c6a25d7b5935ce4f6918b65afe8d6` as the source-review baseline.
- [x] Confirm implementation work starts from current `master`, not an old Local LLM feature branch.
- [x] Confirm the remediation does not change Local-vs-Google product scope.
- [x] Confirm Gemini Live remains the voice-session provider.
- [x] Confirm Moonshine ASR behavior is unchanged unless a specific regression requires otherwise.
- [x] Confirm fully local voice remains deferred.
- [x] Confirm signed/notarized P13 release execution remains owner-deferred.

**Acceptance**

- [x] One implementation/reconciliation record states the exact baseline and scope.
- [x] No task silently expands into local TTS, GPU controls, custom GGUF import, RAG, or tool calling.

## LLMR-002 — Record exactly which prior Local LLM claims are reopened

- [x] Record `LLM-042` reopened for verification-phase cancellation.
- [x] Record `LLM-045` reopened for cancellation semantics and runtime diagnostics exposure.
- [x] Record `LLM-054` reopened because runtime diagnostics are not production reachable.
- [x] Record `LLM-082` reopened because UI Cancel is offered during an uncancellable phase.
- [x] Record `LLM-114` reopened because the UI cancel test proves dispatch rather than end state.
- [x] Record `LLM-152` reopened because the previous silent-failure audit missed acknowledged-but-ignored cancellation.
- [x] Reopen only the “cancellable” portion of the Local LLM Final Gate installer statement.
- [x] Preserve all unaffected prior evidence as historical/accepted.

**Acceptance**

- [x] The records distinguish “previous evidence was false” from “later review discovered a new gap.”
- [x] No prior successful CI/real-model evidence is deleted or rewritten out of history.

## LLMR-003 — Convert the code-review findings into deterministic regression probes

Before or alongside each fix, add a regression that would fail against the baseline where practical.

- [x] verification-phase cancellation probe;
- [x] pre-promotion cancellation race probe;
- [x] verifying-status refresh probe;
- [x] chronological last-error probe;
- [x] HTTPS downgrade redirect probe;
- [x] same-size post-install mutation probe;
- [x] future-settings-version destructive-downgrade probe;
- [x] split-settings-snapshot request race probe;
- [x] stale frontend full-object overwrite probe;
- [x] runtime-diagnostics production reachability probe;
- [x] diagnostics privacy sentinel probe.

**Acceptance**

- [x] Each defect has a test/probe that fails for the intended reason before its repair, or the reconciliation explains why a direct red probe is impractical.
- [x] All probes are offline/hardware-free unless the property inherently requires real-model acceptance.

---

# P1 — Installer Cancellation and Async Correctness

## LLMR-100 — Track truthful in-flight installer phase

- [x] Replace “model ID -> cancellation token only” state with an in-flight record that includes current phase.
- [x] Represent at least `downloading`, `verifying`, and `promoting` or equivalent explicit phases.
- [x] Update phase atomically/consistently as the install advances.
- [x] Keep cancellation token ownership in the same authoritative in-flight record.
- [x] Ensure cleanup removes the in-flight record on success, failure, and cancellation.

**Acceptance**

- [x] A status query while hashing reports `verifying`, not `downloading`.
- [x] A status query during promotion reports a truthful active state.
- [x] Restart/stale-partial behavior remains safe.

## LLMR-101 — Move artifact hashing off the async executor

- [x] Isolate the large synchronous SHA/size verification loop in `spawn_blocking` or an equivalent blocking worker.
- [x] Keep typed verification outcomes when crossing the blocking boundary.
- [x] Do not move network async I/O into unnecessary blocking code.
- [x] Keep memory usage bounded by streaming reads rather than loading the GGUF into memory.

**Acceptance**

- [x] The async install future does not perform the full GGUF hash loop directly on its executor worker.
- [x] Verification of the existing catalog artifacts remains streaming/bounded.
- [x] Tests can deterministically pause verification without blocking the whole async test runtime.

## LLMR-102 — Make SHA verification cooperatively cancellable

- [x] Pass cancellation state into the verification worker.
- [x] Check cancellation once per bounded read chunk or equivalently often.
- [x] Return a typed cancellation result distinct from hash/size/network failures.
- [x] Check cancellation again after the blocking worker returns.
- [x] Do not emit `installed` after a cancellation has been accepted for that operation.

**Acceptance**

- [x] Cancel during hashing terminates without waiting for the entire artifact hash.
- [x] No final model/valid marker remains from the cancelled operation.
- [x] The user-visible state becomes truthfully non-installed/cancelled/failed according to the existing state model.

## LLMR-103 — Close the verification-to-promotion cancellation race

- [x] Add a mandatory cancellation check after successful verification and before artifact promotion.
- [x] Define cancellation semantics if cancellation races the final rename.
- [x] Ensure a renamed artifact without a committed valid marker is not treated as installed.
- [x] Ensure cleanup/restart logic removes or safely ignores any incomplete promoted artifact.
- [x] Add equivalent protection before committing the install marker.

**Acceptance**

- [x] A deterministic barrier test cancels after verification but before promotion and proves no valid install results.
- [x] A deterministic barrier test covers the narrow promotion/marker boundary.
- [x] A later explicit reinstall succeeds cleanly.

## LLMR-104 — Add complete installer cancellation end-state tests

Cover all meaningful phases:

- [x] cancel during download;
- [x] cancel after download before verify;
- [x] cancel during verify;
- [x] cancel after verify before promotion;
- [x] cancel at promotion/marker boundary;
- [x] cancel then reinstall successfully;
- [x] duplicate cancellation remains safe/idempotent where appropriate.

**Acceptance**

For every accepted cancellation:

- [x] final GGUF is absent or explicitly non-installed;
- [x] no valid marker exists;
- [x] staging state is cleaned up;
- [x] status does not later become `installed` for the cancelled operation.

---

# P2 — Installer Integrity, Network, Diagnostics, and Structure

## LLMR-200 — Enforce HTTPS on every model-download redirect

- [x] Replace the generic limited redirect policy with one that validates every target URL.
- [x] Reject any redirect whose target scheme is not `https`.
- [x] Preserve a finite redirect limit.
- [x] Return a safe network/install error category rather than a raw URL-heavy native error.
- [x] Keep initial catalog HTTPS validation.

**Acceptance**

- [x] HTTPS -> HTTPS redirect is accepted within the limit.
- [x] HTTPS -> HTTP redirect is rejected before artifact bytes are trusted.
- [x] Redirect-loop/excess-count behavior remains bounded.
- [x] Tests do not require the public Internet.

## LLMR-201 — Make installer `last_error` chronological

- [x] Stop using unordered `HashMap` iteration as chronology.
- [x] Introduce a monotonic sequence/timestamp or dedicated latest-error record.
- [x] Preserve per-model safe errors if they are useful independently.
- [x] Ensure success for one model does not accidentally misreport an older/newer unrelated error as “latest.”

**Acceptance**

- [x] A deterministic two-model test proves the most recently recorded error is returned.
- [x] Normal diagnostics contain only safe error category/message data.

## LLMR-202 — Reverify installed model bytes before first runtime load

- [x] Define a runtime-use verification cache policy.
- [x] Before first load per process, hash the current GGUF and compare with the pinned catalog SHA unless an equally strong cached verification applies.
- [x] Cache successful verification using canonical path plus conservative file identity/metadata.
- [x] Invalidate cached verification when observed file identity/size/mtime or equivalent metadata changes.
- [x] Fail closed on mismatch before handing the file to llama.cpp.
- [x] Do not hash before every generation call.

**Acceptance**

- [x] Same-size byte mutation after installation is rejected before runtime load.
- [x] Unmodified artifact loads successfully.
- [x] Repeated generations on the same unchanged loaded model do not repeatedly hash the entire GGUF.
- [x] No fallback to Google/Fake/another local model occurs on mismatch.

## LLMR-203 — Reconcile install validity terminology

- [x] Distinguish “marker/shape-valid install” from “current bytes cryptographically verified for runtime use” where necessary.
- [x] Ensure UI/docs do not call a merely size/marker-checked artifact cryptographically verified unless it has been hashed under the current verification policy.
- [x] Keep normal status checks fast enough for UI refresh.

**Acceptance**

- [x] Terminology in code/types/docs matches actual guarantees.
- [x] Fast status queries do not require hashing hundreds of MB on every render.

## LLMR-204 — Refactor installer responsibilities where needed for correctness

- [x] Separate state tracking, download transport, verification, path policy, promotion, deletion, and diagnostics enough that phase invariants are locally understandable.
- [x] Avoid duplicate implementations of path safety, cancellation, or verification policy.
- [x] Keep public command/runtime interfaces stable unless a typed improvement is required.
- [x] Preserve existing adversarial path/symlink/atomic-install tests.

**Acceptance**

- [x] No replacement module becomes another monolithic copy of the same responsibilities.
- [x] Existing installer tests remain green without weakening assertions.
- [x] New cancellation/integrity tests target production paths, not test-only mirrors.

---

# P3 — Complete Runtime Diagnostics

## LLMR-300 — Make `LocalRuntimeDiagnostics` production reachable

- [x] Remove the dead-code status by adding a real production caller.
- [x] Use `LocalRuntimeManager::diagnostics()` or its direct successor as the authoritative runtime source.
- [x] Compose installer and runtime diagnostics into one coherent Local LLM diagnostics response or clearly related responses.
- [x] Expose selected/loaded identity, phase, thread/context configuration, generation state, safe error category, duration/tokens/tokens-per-second when available.
- [x] Do not expose prompt/output text.

**Acceptance**

- [x] A production Tauri command can return live runtime diagnostics.
- [x] The response changes truthfully across unloaded -> loaded -> generating -> idle/unloaded transitions.

## LLMR-301 — Carry runtime diagnostics through the generated IPC contract

- [x] Add Rust representative diagnostics shape/value data to the authoritative exporter.
- [x] Add/update TypeScript interfaces.
- [x] Update frontend IPC shape checker coverage.
- [x] Update Tauri command-registration coverage if command names change/add.
- [x] Update test dispatcher fixtures using real Rust command shapes.

**Acceptance**

- [x] A deliberate Rust-side diagnostics-field drift fails the frontend shape gate.
- [x] All frontend-invoked diagnostics commands are registered in Rust.
- [x] No handwritten unverified JSON cast bypasses the contract gate.

## LLMR-302 — Surface useful Local LLM runtime diagnostics in the UI

- [x] Choose the existing Local LLM settings panel or Diagnostics surface as the UI owner.
- [x] Show selected model ID and loaded model identity.
- [x] Show loaded/unloaded/runtime phase.
- [x] Show thread count and context size.
- [x] Show generation-in-progress state.
- [x] Show safe last-error category.
- [x] Show last duration/token counts/tokens-per-second when available.
- [x] Show truthful installer phase, including verifying.
- [x] Keep prompt/output/memory/transcript contents out of diagnostics.

**Acceptance**

- [x] A user can tell whether the model is installed, loaded, generating, or failed without reading developer logs.
- [x] UI tests cover representative unloaded/loaded/generating/error states.

## LLMR-303 — Add diagnostics privacy and liveness tests

- [x] Inject prompt sentinel.
- [x] Inject output sentinel.
- [x] Inject memory/transcript sentinel where relevant.
- [x] Exercise diagnostics while/after generation.
- [x] Prove sentinels do not appear in diagnostics payload/UI/logging.
- [x] Include positive control proving the diagnostics test actually observed live fields.

**Acceptance**

- [x] Privacy assertions cannot pass vacuously against an empty/unreached diagnostics path.

---

# P4 — Settings Compatibility and Request Consistency

## LLMR-400 — Fail closed on persisted future settings versions

- [x] Read/inspect `settings_version` before destructive normalization.
- [x] Preserve `version < current` migration behavior.
- [x] Preserve `version == current` normal load behavior.
- [x] Reject `version > current` with a typed safe compatibility/startup error.
- [x] Do not persist a downgraded/stripped representation.
- [x] Do not silently replace with defaults.

**Acceptance**

- [x] Fixture with `CURRENT + 1` fails closed.
- [x] Fixture contains an unknown future field and remains byte/semantically unchanged in persistence after the failed load.
- [x] Existing migration/idempotence tests remain green.

## LLMR-401 — Introduce one immutable settings snapshot per text-generation request

- [x] Add an explicit settings snapshot/read API if one does not already exist cleanly.
- [x] Capture the snapshot once at the start of typed text generation.
- [x] Capture the snapshot once at the start of ambient generation.
- [x] Derive provider selection from that snapshot.
- [x] Derive provider-specific model ID from that snapshot.
- [x] Derive memory inclusion/privacy-relevant prompt inputs from that snapshot.
- [x] Derive transcript-retention decision for that request from that snapshot.

**Acceptance**

- [x] No provider factory used by an in-flight request independently re-reads mutable global settings.
- [x] A concurrent settings change affects the next request, not half of the current request.

## LLMR-402 — Add snapshot-aware provider selection

- [x] Refactor `get_text_model()` or add `get_text_model_for(snapshot)`/equivalent.
- [x] Preserve fail-closed Google missing-key behavior.
- [x] Preserve fail-closed Local missing-model/runtime behavior.
- [x] Preserve Fake test injection boundaries without making Fake persistable.
- [x] Avoid copying provider-selection logic into ambient/typed callers.

**Acceptance**

- [x] Provider-neutral caller architecture remains intact.
- [x] Existing no-fallback negative tests continue to pass.

## LLMR-403 — Prove request consistency under forced concurrent settings changes

Use barriers/deferred futures rather than timing sleeps.

- [x] Start request under settings A.
- [x] Pause after snapshot but before provider invocation.
- [x] Change provider/model/privacy/memory-related settings to B.
- [x] Resume request.
- [x] Prove the current request uses internally consistent A semantics.
- [x] Start a second request and prove it uses B.
- [x] Cover typed text path.
- [x] Cover ambient path.

**Acceptance**

- [x] No request sends prompt/memory/provider data assembled from a mixture of A and B.
- [x] Existing post-generation current-state suppression remains intact where appropriate.

---

# P5 — Frontend Settings Write Hardening

## LLMR-500 — Introduce a patch-oriented component settings API

- [x] Add `updateSettingsPatch` or equivalent typed partial-intent API.
- [x] Have the store apply patches to its authoritative current/reconciled base.
- [x] Preserve ordered persistence queue semantics.
- [x] Preserve continuous-control coalescing/debounce semantics.
- [x] Preserve rejected-write reconciliation and safe error handling.

**Acceptance**

- [x] A component changing one field does not submit unrelated stale fields as intent.
- [x] Existing V1R-216/V1R-217 ordering/reconciliation properties remain true.

## LLMR-501 — Migrate settings callers away from stale full-object spreads

- [x] Audit all `updateSettings({ ...settings, ... })` call sites.
- [x] Migrate Local LLM provider/model controls.
- [x] Migrate Google model/settings controls where the same risk applies.
- [x] Migrate other ordinary one/few-field Settings controls.
- [x] Retain complete replacement only where replacement is intentionally the semantic operation.
- [x] Document/justify any remaining complete-object call site.

**Acceptance**

- [x] Code search finds no unjustified stale complete-object update pattern in Settings UI.

## LLMR-502 — Add stale-caller overwrite regression tests

- [x] Render/capture a component with settings snapshot A.
- [x] Advance store/persistence to unrelated newer value B.
- [x] Trigger the stale component's intended field change.
- [x] Prove unrelated B survives.
- [x] Cover a Local LLM provider/model control.
- [x] Cover at least one continuous/discrete interaction if the patch API touches that path.

**Acceptance**

- [x] Tests fail against the stale full-object behavior and pass with patch intent.

---

# P6 — Runtime Cancellation Semantics and Template Truthfulness

## LLMR-600 — Audit normal Local generation cancellation ownership

- [x] Enumerate every creation/use of Local runtime cancellation tokens.
- [x] Identify which are reachable from normal `TextModel::generate()` callers.
- [x] Identify shutdown, model switch, deletion, and any user-triggered cancellation requirements.
- [x] Record whether typed/ambient generation currently needs a first-class external cancellation API.
- [x] Do not claim an externally cancellable path when the token is private/unreachable.

**Acceptance**

- [x] One written decision distinguishes cooperative runtime cancellation from application-exposed cancellation.

## LLMR-601 — Wire a real generation cancellation handle if required by current product semantics

If LLMR-600 finds a current requirement:

- [x] N/A — use manager/request-owned safe cancellation state;
- [x] N/A — preserve decode-iteration cooperative cancellation;
- [x] N/A — integrate with switch/delete/shutdown ordering;
- [x] N/A — avoid unsafe native abort FFI solely for forced cancellation;
- [x] N/A — add deterministic in-flight cancellation tests.

If no current user-facing cancellation requirement exists:

- [x] mark this implementation subtask N/A with rationale;
- [x] correct comments/docs so they claim only shutdown/switch/internal cancellation semantics actually provided.

## LLMR-602 — Correct chat-template ownership comments and documentation

- [x] Audit comments claiming llama.cpp/the native layer applies the embedded GGUF template.
- [x] Document the actual family-specific rendering path.
- [x] Document how the embedded template is used for compatibility validation.
- [x] Document explicit Qwen non-thinking framing/cleanup.
- [x] Keep unsupported templates fail-closed.

**Acceptance**

- [x] Code comments, architecture docs, and tests describe the same ownership model.

## LLMR-603 — Harden supported-template identification

- [x] Evaluate normalized template fingerprinting or an equivalent deterministic discriminator for the two pinned artifacts.
- [x] If implemented, pin/validate the supported fingerprint(s).
- [x] If substring validation is retained, prove/document why the selected checks are sufficient for the exact pinned artifacts and what they do not prove.
- [x] Do not add a disproportionate templating framework solely for this cleanup.

**Acceptance**

- [x] A deliberately incompatible/altered template fixture fails closed.
- [x] Existing SmolLM2 and Qwen family fixtures remain accepted.
- [x] If actual prompt framing changes, schedule P12 rerun before final closure.
---

# P7 — Adversarial, Concurrency, and UI Regression Matrix

## LLMR-700 — Expand installer adversarial matrix

Add/retain explicit coverage for:

- [x] wrong SHA;
- [x] same-size wrong SHA;
- [x] wrong byte count;
- [x] truncated response;
- [x] oversized response;
- [x] download cancellation;
- [x] verification cancellation;
- [x] pre-promotion cancellation;
- [x] stale temp file;
- [x] traversal filename;
- [x] symlink/path escape;
- [x] atomic promotion failure;
- [x] duplicate concurrent install;
- [x] HTTPS downgrade redirect;
- [x] post-install same-size mutation.

**Acceptance**

- [x] Every negative case fails safely without selecting another provider/model.

## LLMR-701 — Replace superficial frontend Cancel test with end-state proof

- [x] Keep assertion that `cancelLocalLlmModelInstall` is invoked.
- [x] Do not satisfy the test by resolving the same cancelled operation as `installed`.
- [x] Simulate/drive verifying phase.
- [x] Cancel.
- [x] Prove UI eventually shows non-installed/cancelled/failed-safe state.
- [x] Prove no stale optimistic installed state survives.
- [x] Prove a later new install can succeed.

**Acceptance**

- [x] Test would fail if backend/UI again acknowledges cancellation and then installs the same operation.

## LLMR-702 — Add request-snapshot concurrency matrix

- [x] Local -> Google switch during typed request.
- [x] Google -> Local switch during typed request.
- [x] local model A -> B switch during request where applicable.
- [x] memory setting change during prompt construction.
- [x] transcript-retention change during request.
- [x] ambient privacy/behavior setting change during generation.

**Acceptance**

- [x] Current request is internally consistent; next request observes new state.

## LLMR-703 — Add diagnostics truthfulness matrix

- [x] not installed;
- [x] downloading;
- [x] verifying;
- [x] installed/unloaded;
- [x] loading/loaded where phase exists;
- [x] generating;
- [x] generation success metrics;
- [x] safe runtime error;
- [x] unload/delete;
- [x] chronological installer errors.

**Acceptance**

- [x] UI/IPC values are sourced from production state rather than test-only mirrors.

---

# P8 — Contract, CI, and Cross-Platform Integrity

## LLMR-800 — Reconcile generated frontend contract after diagnostics/settings API changes

- [x] Regenerate authoritative backend contract.
- [x] Ensure diagnostics representative shapes are included.
- [x] Ensure any new settings/version error/category shapes are represented if IPC-visible.
- [x] Run frontend shape checker.
- [x] Run generated-contract drift gate.
- [x] Add mutation proof for new diagnostics field coverage if existing mutation machinery can be extended cleanly.

**Acceptance**

- [x] Rust/TypeScript diagnostics shapes cannot silently drift.

## LLMR-801 — Reconcile Tauri command registration/fixtures

- [x] Add/register any new Local diagnostics command or update the existing one.
- [x] Update `tauriBridge` command invocation.
- [x] Update test dispatcher fixtures.
- [x] Run command registration checker.
- [x] Demonstrate negative command-name mismatch fails, then revert.

**Acceptance**

- [x] All frontend-invoked command names are registered.

## LLMR-802 — Preserve model-weight-free ordinary CI

- [x] Ensure new integrity tests use temporary/small fixtures rather than downloading real GGUFs.
- [x] Ensure diagnostics tests do not require real model weights.
- [x] Keep compile proofs for Linux/macOS architectures.
- [x] Keep unsigned macOS bundle smoke jobs green.
- [x] Keep dependency/security/license gates green.

**Acceptance**

- [x] Ordinary PR/master CI downloads zero catalog GGUF model weights.

## LLMR-803 — Decide whether P12 real-model acceptance must be rerun

- [x] Compare final remediation diff against model identity/runtime/template/generation semantics.
- [x] If none changed materially, record why existing P12 evidence remains applicable.
- [x] If template/runtime/load/generation semantics changed, rerun the affected SmolLM2/Qwen acceptance before final closure.
- [x] Do not rerun expensive real-model acceptance merely for UI/docs/state bookkeeping changes.

**Acceptance**

- [x] Reconciliation contains an explicit P12 rerun/not-required decision with rationale.

---

# P9 — Documentation and Historical Reconciliation

## LLMR-900 — Add a post-review addendum to the original Local LLM tracker

- [x] Do not erase historical `[x]` evidence from `docs/TODO(20260831-081800).md`.
- [x] Add a clear post-review note/cross-reference stating which claims were reopened by `LLMR` findings.
- [x] Point to this TODO/spec as the active remediation authority.
- [x] After remediation, record how each reopened claim was reclosed.

**Acceptance**

- [x] A reader cannot mistake the old Final Gate line for the current unresolved status while remediation is active.
- [x] Historical evidence remains readable.

## LLMR-901 — Update Local LLM reconciliation with post-review findings and fixes

- [x] Add the 2026-09-05 code-review finding inventory.
- [x] Record exact fixes and tests for each finding.
- [x] Record affected PRs/SHAs/CI runs.
- [x] Record P12 rerun decision/evidence.
- [x] Record residual limitations after fixes.
- [x] Preserve P13 packaging/license evidence and owner-deferred signed release status.

**Acceptance**

- [x] Reconciliation distinguishes source inspection, deterministic tests, CI, real-model evidence, and deferred physical/release evidence.

## LLMR-902 — Update developer/architecture documentation for changed semantics

As applicable:

- [x] request settings snapshot ownership;
- [x] Local runtime diagnostics path;
- [x] installer verification/cancellation phases;
- [x] runtime-use artifact verification policy;
- [x] future settings-version fail-closed policy;
- [x] patch-oriented frontend settings writes;
- [x] generation cancellation semantics;
- [x] chat-template ownership.

**Acceptance**

- [x] Documentation describes the production code, not aspirational or obsolete behavior.

## LLMR-903 — Publish explicit residual limitations and deferred gates

- [x] Preserve cooperative/native cancellation limitations that still genuinely apply.
- [x] Preserve bounded shutdown semantics.
- [x] Preserve CPU-only V1 statement.
- [x] Preserve first-token-latency limitation if still applicable.
- [x] Preserve fully-local-voice deferral.
- [x] Record signed/notarized P13 release candidate as owner-deferred.
- [x] Record physical Mac and human voice acceptance as deferred, not silently passed by CI.

---

# P10 — Final Remediation Gate

## LLMR-1000 — Run canonical local/static verification available in the implementation environment

- [x] `git diff --check` passes.
- [x] frontend formatting/lint/typecheck/tests pass where dependencies/toolchain are available.
- [x] Rust fmt/Clippy/tests pass where Rust toolchain is available.
- [x] generated frontend contract is current.
- [x] frontend IPC shape checker passes.
- [x] Tauri command registration checker passes.
- [x] no generated dependency/build trees are tracked.

**Acceptance**

- [x] Any unavailable local gate is explicitly left for CI rather than falsely claimed.

## LLMR-1001 — Pass exact-head ordinary CI

On the final remediation PR head:

- [x] Frontend quality passes.
- [x] Rust quality passes.
- [x] Dependency audit passes.
- [x] Security audit passes.
- [x] Release metadata/static gate passes.
- [x] Linux Local LLM compile proof passes.
- [x] macOS arm64 Local LLM compile proof passes.
- [x] macOS x86_64 Local LLM compile proof passes.
- [x] macOS arm64 unsigned bundle smoke passes.
- [x] macOS x86_64 unsigned bundle smoke passes.
- [x] literal `npm run check:all` passes after clean `npm ci`.

## LLMR-1002 — Perform final focused source audit against every remediation invariant

- [x] Re-read installer cancellation path end-to-end.
- [x] Re-read artifact verification/cache path end-to-end.
- [x] Re-read settings version path end-to-end.
- [x] Re-read typed request snapshot/provider path end-to-end.
- [x] Re-read ambient request snapshot/provider path end-to-end.
- [x] Re-read frontend patch write/reconciliation path end-to-end.
- [x] Re-read diagnostics Rust -> contract -> TypeScript -> UI path end-to-end.
- [x] Re-read chat-template/runtime cancellation comments against actual code.
- [x] Search for new silent Fake/cloud/local fallback paths.
- [x] Search for prompt/output/credential leakage in new diagnostics/errors.

**Acceptance**

- [x] No mandatory defect from `SPEC(20260905-141500).md` remains unresolved.

## LLMR-1003 — Merge, verify exact master, and reclose the Local LLM Final Gate

- [x] Merge only after exact final PR head CI succeeds.
- [x] Use an expected-head guard for the merge.
- [x] Verify resulting exact `master` SHA.
- [x] Verify ordinary CI succeeds on that exact merged `master` SHA.
- [x] Update reconciliation with final exact evidence without creating an evidence-recursion commit solely to record its own CI.
- [x] Reclose `LLM-042`, `LLM-045`, `LLM-054`, `LLM-082`, `LLM-114`, `LLM-152` through the post-review addendum/evidence.
- [x] Restore the installer-cancellable Final Gate statement only after cancellation end-state tests pass.

---

# Final Remediation Gate Checklist

- [x] All `LLMR-###` tasks required by the chosen implementation are complete.
- [x] Installer cancellation is truthful during download, verification, and pre-promotion races.
- [x] Accepted cancellation cannot later produce `installed` for the same operation.
- [x] GGUF hashing is isolated from the async executor.
- [x] Status refresh preserves the true `verifying` phase.
- [x] Installer latest-error reporting is chronological.
- [x] HTTPS downgrade redirects fail closed.
- [x] Same-size post-install tampering is detected before runtime load.
- [x] Runtime diagnostics are reachable through production typed IPC/UI.
- [x] Diagnostics expose no prompt/output/memory/transcript/credential payloads.
- [x] Future settings versions fail closed without rewrite.
- [x] One request uses one immutable settings/provider/privacy snapshot.
- [x] Frontend components express settings patches rather than stale full-object intent.
- [x] Runtime generation cancellation semantics are truthful and documented.
- [x] Chat-template ownership and compatibility checks are truthful and tested.
- [x] Existing provider no-fallback/Fake-isolation/privacy/path-safety guarantees remain intact.
- [x] Ordinary CI remains real-model-weight-free.
- [x] P12 real-model evidence is either explicitly retained with rationale or rerun where semantics changed.
- [x] Canonical `npm run check:all` passes from clean dependencies on the final exact head.
- [x] Final exact merged `master` CI passes.
- [x] Historical Local LLM records contain a clear post-review reopening/reclosure addendum.
- [x] Signed/notarized P13 release execution and physical/human macOS acceptance remain explicitly deferred rather than misclassified as complete.