# Talking Moose AI — Defect Remediation TODO

**Version:** 2026-08-24 14:22 PDT, compact continuation 2026-08-28
**Status:** Active remediation plan — code-review defect closure
**Companion specification:** `SPEC(20260824-142233).md`
**Full historical snapshot through V1R-183 closure:** commit `8346e19cdc69be5ff09613c7274e7ed539cb6e36`

This file remains the authoritative active P14–P20 remediation tracker. Git history at `8346e19cdc69be5ff09613c7274e7ed539cb6e36` preserves all completed rows and their detailed implementation evidence through V1R-183; this compact continuation keeps the still-open work plus closures completed afterward.

## Tracker rules

- `[x]` means the criterion is implemented **and** proven by a test that exercises the production path.
- `[ ]` means implementation or proof remains.
- A `#[cfg(test)]` reimplementation of a production branch does not close a row.
- Concurrency rows require the interleaving to be exercised.
- Privacy rows require output capture from the subsystem that handles the sensitive data.
- Gate P18 remains open until V1R-182 completes the vacuous/mis-cited-test audit.

---

# Closed since the archived snapshot

## V1R-197 — Tool audit observability — **S3**

`tools/router.rs`, `commands/tool_diagnostics.rs`, `lib.rs`, `src/lib/tauriBridge.ts`, `src/components/Settings/`

- [x] The audit trail is reachable by the user or diagnostics.
- [x] `duration_ms` is observable.

Implementation evidence (2026-08-28): production observability landed in `d7db34273171f31ad84de791d517013aae10aa9f` (`fix: expose tool audit diagnostics`). `ToolRouter::dispatch` records bounded metadata-only `ToolAuditRecord` entries including `duration_ms`; `commands::tool_diagnostics::get_tool_audit` returns the router's real `audit_snapshot()` and is registered in the Tauri invoke handler. The frontend `tauriBridge.getToolAudit()` invokes that command, Settings exposes a user-reachable Diagnostics tab, and `ToolAuditDiagnosticsPanel` renders the latest session-local records with Time, Tool, Permission, Result, and Duration columns, including `duration_ms` as milliseconds. The Rust regression `diagnostics_expose_metadata_only_audit_records_with_duration` dispatches through the real router, reads the production snapshot helper used by the command, proves `duration_ms` serializes, and proves raw arguments/results are absent. The frontend regression renders the production diagnostics panel and proves execution duration is visible. GitHub Actions run `33164745601` passed on `3cd911af5c7446bc77c90e4fb95759257a843127`, executing both the Rust and frontend suites after this production wiring was already present. Focused local frontend validation also passed 17/17 tests across the tool-audit panel and Settings modal.

## V1R-200 — Agent guidance accuracy — **S3**

`AGENTS.md`

- [x] The speech-output section describes the shipped path (Google TTS + Rust playback), not the removed `espeak`/`say` subprocess path.
- [x] A sweep confirms no other section misdescribes current architecture.

Implementation evidence (2026-08-28): the original architecture correction landed in `531cd41a18048d6dea690eec55f741d7a458fddc`, replacing the obsolete system-speech/`espeak`/`say` guidance with the shipped `GoogleSpeechSynthesizer` → `audio/speech.rs` → Rust `AudioPlayback`/`cpal` path and correcting Gemini Live framing/model guidance. The completion audit found one remaining developer-guide defect: `npm run check:all` was mislabeled as a frontend-only gate and the duplicated hand-written Clippy/test commands omitted the repository's required `--all-targets --all-features` coverage. `d057c614e8ae3aeeb66cdf5a32cdf6274e99357d` (`docs: finish agent architecture guidance audit`) now makes the package scripts the verification source of truth, explicitly documents standalone Gemini TTS, provider-neutral/Moonshine ASR, conversation lifecycle, desktop observation, SQLite persistence, and bounded/privacy-gated tool routing, and narrows the Tauri async-runtime note to Tauri-owned background tasks. The source sweep cross-checked React/Tauri versions, `decorations: false`, ASR/desktop/persistence module layout, window-drag permission plus `startDragging()`, current Gemini model IDs, Live JSON/binary-frame decoding, and the CI Rust flags against the production files. Repository search found no remaining shipped `espeak`/`say` fallback guidance. The edited `AGENTS.md` passed `git diff --check` and Prettier locally; this task changes developer documentation only.

## V1R-160 — Speech command de-duplication — **S4**

`commands/ambient.rs`, `commands/character.rs`, `commands/conversation/core.rs`, `commands/speech.rs`, `commands/presentation.rs`, `audio/speech.rs`

- [x] One speech-invocation helper replaces the three current implementations.
- [x] All three paths agree on what constitutes synthesis failure (`queued_samples == 0`).
- [x] One `transition_and_emit` helper replaces the three identical copies.
- [x] One `show_character` helper replaces the two byte-identical copies.
- [x] Test covers zero-playable-audio on every speech path.

Implementation evidence (2026-08-28): the consolidation landed in `3ef40ab4d8e8766f401c4abf5175e3b93664f25a` and stale-completion ownership was hardened in `b7cc4157e6ce370f517ce5bf0df2393f7c98a061`. `commands::speech::invoke_standalone_speech` is the single standalone-speech boundary used by ambient delivery, character reactions/voice auditions, and text-mode conversation replies. Its production `synthesize_standalone` path treats `queued_samples == 0` as `NO_PLAYABLE_STANDALONE_AUDIO` before surfacing Talking/bubble state and also fails closed on queue overflow. `commands::presentation` contains the sole definitions of `transition_and_emit` and `show_character`; ambient/character/conversation code imports those helpers rather than retaining copies. `zero_playable_audio_fails_identically_for_every_standalone_speech_path` exercises the shared production synthesis helper for ambient, character, and conversation inputs and proves zero playable audio leaves playback stopped and empty. The ownership regression `cancelled_completion_never_claims_the_newer_utterance_finished` proves stale completion cannot clear a newer utterance. GitHub Actions run `33164745601` passed on `3cd911af5c7446bc77c90e4fb95759257a843127`; its Rust quality job successfully ran Rustfmt, Clippy, and `cargo test --all-targets --all-features`, so these exact Rust tests executed successfully. A compare from that tested commit to current master shows only documentation changed afterward, so the tested V1R-160 production/test sources are identical to current master.

## V1R-161 — Platform stub compilation and coverage — **S4**

`desktop/mod.rs`, `desktop/macos.rs`, `desktop/macos/non_macos.rs`

- [x] Every platform implementation of the observer contract is compiled in CI.
- [x] A shared contract test runs against each implementation, or the redundant implementation is deleted.

Implementation evidence (2026-08-28): `2e7a3791d8d744b58a6805a152a8e2b6fdb71c79` (`fix: unify desktop observer fallback compilation`) deleted the redundant `desktop/macos_stub.rs` production implementation and made `desktop::macos` always resolve to `macos.rs`. That module selects the real macOS platform implementation under `target_os = "macos"` and the fail-closed `macos/non_macos.rs` branch otherwise, so Linux tests and Linux production compile the same fallback. The existing `unsupported_platform_never_fabricates_observer_values` regression therefore exercises the production non-macOS branch rather than a test-only alternative. Follow-up commits `0c3fd2f736d85ab5c2d035e9f308a0825230faee` and `fae2755b80b425ec5842b5368629efee0eceea21` gated macOS-only normalization helpers/imports to macOS-or-test builds and kept Rustfmt clean without lint suppression. GitHub Actions run `33169999554` passed on exactly `fae2755b80b425ec5842b5368629efee0eceea21`: the Linux Rust quality job passed Rustfmt, Clippy, Rust tests, and both backend matrices, while the arm64 and x86_64 macOS Tauri bundle jobs also passed. This executes/compiles the unified non-macOS production path on Linux and the real macOS implementation on both supported architectures.

---

# Remaining remediation work

## V1R-162 — Character state model — **S4**

`character/state.rs`, `app/state.rs`, `commands/character.rs`, `src/lib/sprites.ts`

- [ ] `CharacterState::Annoyed` and `::Sleeping` are either driven by real behavior or removed along with their frontend sprite support.
- [ ] Mute has a single authoritative representation, or the invariant binding `CharacterState::Muted` to the `Arc<RwLock<bool>>` ambient flag is enforced in one place.
- [ ] Test proves the two mute representations cannot diverge.

## V1R-166 — Conversion de-duplication — **S4**

`audio/playback.rs`, `audio/resample.rs`

- [ ] One f32↔i16 conversion implementation.

## V1R-182 — Vacuous and mis-cited tests — **S4**

`app/tray.rs`, `docs/RECONCILIATION_P12_20260823.md`

- [ ] `tray_actions_are_explicit_and_bounded` asserts a real property rather than the length of its own literal.
- [ ] The P12 reconciliation cites the test that actually asserts barge-in flush behavior, not an empty-queue test.
- [ ] Sweep for other tests asserting properties of their own literals.

### Gate P18

- [ ] No privacy or correctness claim in either TODO rests on a test that cannot fail.

V1R-183 is closed; Gate P18 remains open until V1R-182 completes the vacuous/mis-cited-test sweep and reconciles the remaining citations.

## V1R-201 — Reconciliation and tracker accuracy — **S4**

`docs/RECONCILIATION_*.md`, `docs/TODO(20260818-163801).md`

- [ ] Reconciliation documents cite tests that assert what is claimed.
- [ ] The feature TODO header retains the §7.3 warning that `[x]` rows are claims to verify.

---

# Cross-cutting definition of done

For every row marked complete:

- [ ] Defect repaired in production code, not worked around at a call site.
- [ ] Test exercises the production path, not a `#[cfg(test)]` mirror.
- [ ] Concurrency rows: test exercises the interleaving.
- [ ] Privacy rows: proof captures output from the subsystem handling that data class.
- [ ] `npm run check:all` passes.
- [ ] No new lint suppression added to make the change pass.

---

# Closed remediation summary

The full tracker at commit `8346e19cdc69be5ff09613c7274e7ed539cb6e36` contains the complete row-level evidence for previously closed work. Closed task IDs include V1R-140–147, V1R-153, V1R-154, V1R-160, V1R-161, V1R-163–165, V1R-170, V1R-171, V1R-180, V1R-181, V1R-183, V1R-190–197, and V1R-200. V1R-194 and V1R-196 are closed under their documented V1 decisions.

# Recommended next task

**Next: V1R-162 (character state model, S4).**

Continue the S4 rows, then V1R-201 final reconciliation. Gate P18 remains open until V1R-182 closes the vacuous/mis-cited-test audit.
