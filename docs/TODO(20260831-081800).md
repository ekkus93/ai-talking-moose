# AI Talking Moose — Local LLM Provider TODO

**Companion spec:** `docs/SPEC(20260831-081800).md`
**Scope:** Local CPU-friendly text-generation provider alongside Gemini; fully local voice is deferred.

Task IDs use the `LLM-###` prefix.

---

## P0 — Architecture and Baseline Reconciliation

### LLM-001 — Freeze the local-LLM V1 boundary

- [ ] Record the current authoritative `master` SHA before implementation.
- [ ] Confirm Local LLM V1 covers ambient remarks and typed `send_text_message` generation only.
- [ ] Confirm Gemini Live remains the voice-session provider in this phase.
- [ ] Confirm Moonshine local ASR behavior is unchanged by this phase.
- [ ] Confirm fully local voice is explicitly deferred.

**Acceptance**

- [ ] Developer docs contain one unambiguous diagram for Local Text V1 and one for the deferred local-voice path.
- [ ] No implementation task requires `LocalTextModel` to implement `RealtimeConversationProvider`.

### LLM-002 — Audit every current use of the generic AI `provider` setting

- [ ] Enumerate Rust reads/writes.
- [ ] Enumerate TypeScript reads/writes.
- [ ] Enumerate generated-contract fields/fixtures.
- [ ] Enumerate tests depending on `provider = "fake"`.
- [ ] Classify Fake provider usage as test-only, browser-preview-only, or production-visible.
- [ ] Identify every current Google-without-key path that manufactures Fake behavior.

**Acceptance**

- [ ] Audit is recorded in implementation notes/reconciliation.
- [ ] No production fallback is left unidentified.

### LLM-003 — Choose and document the llama.cpp integration mechanism

- [ ] Evaluate maintained Rust bindings versus a bundled subprocess design.
- [ ] Require supported macOS arm64/x86_64 builds.
- [ ] Require CPU-only operation.
- [ ] Require normal packaged-app operation without a separately installed llama.cpp executable.
- [ ] Record native dependency/license implications.
- [ ] Record cancellation/thread-safety constraints of the chosen API.

**Acceptance**

- [ ] One mechanism is selected with rationale.
- [ ] A minimal compile-only proof builds in CI before deeper runtime work begins.

---

## P1 — Settings Schema and Migration

### LLM-010 — Introduce a typed text-provider setting

- [ ] Add Rust `TextProvider` enum or equivalent authoritative typed representation.
- [ ] Support at least `google` and `local`.
- [ ] Do not expose `fake` as a normal production provider choice.
- [ ] Add corresponding TypeScript type.
- [ ] Add generated-contract representative shape/value coverage.

### LLM-011 — Split provider-specific text model selections

- [ ] Replace ambiguous `text_model` ownership with `google_text_model` and `local_text_model` or an equivalent unambiguous schema.
- [ ] Preserve `live_model` and `tts_model` independently.
- [ ] Keep local model ID stable and app-owned rather than using arbitrary filenames as persisted identity.

### LLM-012 — Bump settings version and migrate existing profiles

- [ ] Existing `text_model` migrates to `google_text_model`.
- [ ] Existing profiles migrate to `text_provider = google`.
- [ ] Initialize `local_text_model` to the recommended local catalog ID without marking it installed.
- [ ] Preserve all unrelated settings.
- [ ] Migration is idempotent.
- [ ] Normalized settings are persisted once.

**Acceptance**

- [ ] Legacy-current fixture migrates deterministically.
- [ ] Already-migrated fixture does not report another migration.
- [ ] Existing Google model choice survives exactly.
- [ ] Generated frontend contract and TypeScript interface agree.

### LLM-013 — Decide and encode the new-profile default

- [ ] Prefer Local text generation for new profiles unless implementation evidence gives a strong reason not to.
- [ ] If Local is default, selecting the model MUST NOT automatically download it.
- [ ] Onboarding/settings must explain the required model install.
- [ ] If Google remains default, record rationale explicitly.

---

## P2 — Remove Unsafe Provider Fallbacks

### LLM-020 — Make `AppState::get_text_model()` fail explicit rather than manufacture Fake success

- [ ] Remove production Google-without-key -> `FakeTextModel` behavior.
- [ ] Select Google only when `text_provider = google`.
- [ ] Select Local only when `text_provider = local`.
- [ ] Preserve explicit fake injection for unit tests/browser-preview infrastructure only.
- [ ] Ensure missing Google credential returns safe auth/setup failure.
- [ ] Ensure missing local model returns safe model/setup failure.

**Acceptance**

- [ ] Test proves Google without credential never calls FakeTextModel.
- [ ] Test proves Local missing model never calls Google.
- [ ] Test proves local runtime failure never calls Fake or Google.

### LLM-021 — Audit Fake provider production reachability

- [ ] Prove packaged/native production cannot select Fake through persisted ordinary settings.
- [ ] Keep browser preview explicit and development-scoped if Fake behavior remains there.
- [ ] Add regression test for production reachability boundary.

---

## P3 — Local Model Catalog

### LLM-030 — Define authoritative local model descriptor types

Include at minimum:

- [ ] stable model ID;
- [ ] display name;
- [ ] family;
- [ ] parameter scale;
- [ ] quantization;
- [ ] artifact filename;
- [ ] source URL;
- [ ] pinned revision;
- [ ] expected bytes;
- [ ] SHA-256;
- [ ] license metadata;
- [ ] context limit;
- [ ] recommended max output;
- [ ] template/runtime hints if required.

### LLM-031 — Add SmolLM2-360M-Instruct Q4_K_M catalog entry

- [ ] Verify authoritative artifact source.
- [ ] Pin immutable revision.
- [ ] Verify exact filename.
- [ ] Verify expected byte count.
- [ ] Compute/verify SHA-256 independently.
- [ ] Record license and notice requirements.
- [ ] Record supported chat-template behavior in chosen llama.cpp runtime.

**Do not close this task from guessed metadata.**

### LLM-032 — Add a second supported catalog entry

Target Qwen3-0.6B Q4_K_M or the closest current compatible instruct/chat artifact.

- [ ] Pin/verify artifact metadata as above.
- [ ] Confirm non-thinking behavior/template configuration.
- [ ] Confirm CPU load/generation works with the chosen runtime.

### LLM-033 — Add catalog validation tests

- [ ] IDs unique.
- [ ] Filenames path-safe.
- [ ] SHA values syntactically valid.
- [ ] Expected bytes nonzero/reasonable.
- [ ] Revisions nonempty and pinned.
- [ ] Default selected local ID exists.
- [ ] License/source metadata present.

---

## P4 — Local Model Storage and Installer

### LLM-040 — Create local LLM model-root policy

- [ ] Derive an app-managed LLM model root from application data location.
- [ ] Use a layout scoped by model ID and revision.
- [ ] Keep test roots temporary and isolated.
- [ ] Prevent path traversal from catalog fields.

### LLM-041 — Implement install-state model

- [ ] `not_installed` or equivalent.
- [ ] `downloading`.
- [ ] `verifying`.
- [ ] `installed`.
- [ ] `failed` with safe error category/message.
- [ ] Define behavior after restart with stale partial files.

### LLM-042 — Implement verified download

- [ ] Explicit user action required.
- [ ] HTTPS-only source.
- [ ] Temporary/staging file.
- [ ] Bounded declared size.
- [ ] Progress events.
- [ ] Cancellation.
- [ ] SHA-256 verification.
- [ ] Expected-size verification.
- [ ] Atomic final install.
- [ ] Cleanup on failure/cancel.

### LLM-043 — Add adversarial installer tests

- [ ] wrong SHA;
- [ ] wrong byte count;
- [ ] truncated response;
- [ ] oversized response;
- [ ] interrupted/cancelled download;
- [ ] stale temp file;
- [ ] malicious/path-traversal catalog filename;
- [ ] atomic rename/install failure;
- [ ] duplicate concurrent install attempt.

### LLM-044 — Implement model deletion

- [ ] Delete installed artifact and model-owned directory only.
- [ ] Coordinate with loaded/active inference.
- [ ] Unload before deletion.
- [ ] Define selected-but-deleted UI/settings behavior.
- [ ] Never silently select another model.

### LLM-045 — Expose model lifecycle commands through Tauri

Likely commands or equivalent:

- [ ] list local LLM models/status;
- [ ] install model;
- [ ] cancel install;
- [ ] delete model;
- [ ] get local LLM diagnostics/status;
- [ ] test selected local model.

- [ ] Add all command names to IPC registration-contract verification.
- [ ] Add frontend fixtures and production-path tests.

---

## P5 — llama.cpp Runtime Integration

### LLM-050 — Add isolated local runtime module

- [ ] Create `src-tauri/src/ai/local/` or equivalent focused module tree.
- [ ] Keep binding-specific types out of `AppState`, command modules, and frontend-facing types.
- [ ] Add runtime manager ownership/lifecycle.

### LLM-051 — Implement model load/unload

- [ ] Lazy load selected model on first generation/test.
- [ ] Verify install state before opening artifact.
- [ ] Configure CPU execution.
- [ ] Configure bounded context.
- [ ] Configure conservative thread count.
- [ ] Release model/context memory on unload.
- [ ] Sanitize runtime errors.

### LLM-052 — Implement shared runtime synchronization

- [ ] Serialize generation unless chosen runtime is proven safe for concurrency.
- [ ] Prevent delete/switch while the runtime is using the artifact.
- [ ] Prevent use-after-unload races.
- [ ] Define application-shutdown behavior.

### LLM-053 — Implement request cancellation/bounds

- [ ] Bound output tokens.
- [ ] Bound context/prompt size according to explicit policy.
- [ ] Provide cancellation/interrupt if runtime API supports it.
- [ ] Ensure app shutdown cannot hang indefinitely on inference.

### LLM-054 — Add safe runtime diagnostics

Record without prompt/output text:

- [ ] selected model ID;
- [ ] revision/quantization;
- [ ] loaded state;
- [ ] thread count;
- [ ] context size;
- [ ] last load/generation error category;
- [ ] last generation duration;
- [ ] token counts/tokens-per-second if available.

---

## P6 — `LocalTextModel`

### LLM-060 — Implement `TextModel` for local inference

- [ ] Accept `TextRequest` unchanged from callers.
- [ ] Apply system instruction correctly.
- [ ] Apply user prompt correctly.
- [ ] Apply temperature when supported.
- [ ] Apply bounded max output.
- [ ] Return provider-neutral `TextResponse`.
- [ ] Map failures to safe `ProviderError` values.

### LLM-061 — Implement chat-template policy

- [ ] Prefer model/runtime-provided chat template.
- [ ] Do not hand-concatenate raw control tokens at ambient/chat call sites.
- [ ] Fail explicitly when template/runtime compatibility is missing.
- [ ] Add fixture tests for each supported model family.

### LLM-062 — Disable thinking/reasoning mode for supported models where applicable

- [ ] Configure Qwen-family non-thinking behavior if used.
- [ ] Ensure hidden reasoning is not returned in `TextResponse`.
- [ ] Ensure hidden reasoning is not logged/persisted/spoken.

### LLM-063 — Define local text output policy

- [ ] Ambient remains ~60 requested tokens and 320 delivered characters.
- [ ] Typed local responses use a concise local-specific output cap (~128–192 target unless testing changes it).
- [ ] Gemini output policy remains independently configurable/current.
- [ ] Empty local output is treated as failure/empty result according to explicit policy, never manufactured success.

---

## P7 — Provider Routing

### LLM-070 — Route ambient remarks through selected text provider

- [ ] No caller-specific provider branching; continue using `TextModel` abstraction.
- [ ] Preserve both privacy gates.
- [ ] Preserve character-state transitions.
- [ ] Preserve output bounds.
- [ ] Preserve existing TTS/speech controller.

### LLM-071 — Route typed `send_text_message` through selected text provider

- [ ] Preserve message input bound.
- [ ] Preserve memory privacy.
- [ ] Preserve system prompt.
- [ ] Preserve transcript-retention policy.
- [ ] Preserve muted behavior.
- [ ] Preserve speech synthesis path.
- [ ] Restore Idle correctly on local failure.

### LLM-072 — Prove Gemini Live voice remains independent

- [ ] `start_conversation()` behavior unchanged for Local text selection.
- [ ] `get_live_provider()` does not inspect Local text selection to choose Fake/local.
- [ ] Moonshine finalized transcripts continue to use the existing Live provider in this phase.
- [ ] Add regression test documenting this boundary.

---

## P8 — Frontend AI Settings UX

### LLM-080 — Refactor Gemini-only AI tab into provider-aware sections

- [ ] Add Text Generation provider selector: Local / Google Gemini.
- [ ] Separate Text Generation controls from Voice Conversation controls.
- [ ] Avoid presenting Gemini API key as mandatory when Local text is selected.

### LLM-081 — Add local model selector and status

For each model show:

- [ ] display name;
- [ ] approximate download size;
- [ ] installed/not-installed state;
- [ ] recommended/smallest quality hint;
- [ ] selected state;
- [ ] download/install progress;
- [ ] safe failure state.

### LLM-082 — Add install/cancel/delete actions

- [ ] Download button for uninstalled selected model.
- [ ] Progress display.
- [ ] Cancel during download where supported.
- [ ] Delete installed model.
- [ ] Confirm destructive delete if consistent with existing UX conventions.
- [ ] Do not silently switch selection on delete.

### LLM-083 — Add “Test Local Model” action

- [ ] Requires installed selected model.
- [ ] Runs short bounded prompt.
- [ ] Works with network denied after installation.
- [ ] Displays safe success/failure result.
- [ ] Does not leak generated test content into logs.

### LLM-084 — Preserve Google controls

- [ ] API-key save/remove remains secure-store-backed.
- [ ] Gemini text-model selector uses `google_text_model`.
- [ ] Gemini Live model selector remains available for voice.
- [ ] Existing Google connection test remains Google-specific.

### LLM-085 — Add cloud/local disclosure text

- [ ] Clearly state Local affects text generation in this phase.
- [ ] Clearly state Gemini Live voice is still cloud-based.
- [ ] Clearly state Google TTS may still receive locally generated text if selected.
- [ ] Keep privacy text synchronized with actual settings.

---

## P9 — Privacy, Security, and Fail-Closed Behavior

### LLM-090 — Prove no network I/O during local generation

- [ ] Run local TextModel path under repository network-denial harness.
- [ ] Verify generation succeeds through injected/test runtime without network.
- [ ] Verify no Google request builder/client is reached.
- [ ] Model download is tested separately as explicit network behavior.

### LLM-091 — Add local-LLM log privacy tests

Prove logs do not contain:

- [ ] typed prompt sentinel;
- [ ] system prompt sentinel;
- [ ] memory sentinel;
- [ ] ambient summary sentinel;
- [ ] model output sentinel.

- [ ] Keep capture tests non-vacuous using established liveness harness.

### LLM-092 — Sanitize local runtime failures

- [ ] Raw runtime/binding errors stay internal unless demonstrably safe.
- [ ] Frontend receives stable safe categories/messages.
- [ ] Paths and prompt text are excluded from user-visible errors.

### LLM-093 — Harden artifact paths

- [ ] Catalog IDs cannot traverse directories.
- [ ] Filenames cannot be absolute or escape root.
- [ ] Symlink behavior is explicitly safe during install/delete.
- [ ] Delete cannot remove parent/unrelated files.

### LLM-094 — Audit model license/download policy

- [ ] SmolLM2 entry license verified.
- [ ] Qwen entry license verified.
- [ ] Runtime/binding license included in shipped dependency inventory.
- [ ] Any required model notices documented.

---

## P10 — Generated Contract and IPC Integrity

### LLM-100 — Extend backend contract exporter

- [ ] New settings fields emitted.
- [ ] New local model descriptor/status/diagnostics shapes emitted.
- [ ] Representative instances cover newly added IPC-crossing types.
- [ ] JSON remains owned by Rust exporter and passes exact drift gate.

### LLM-101 — Extend TypeScript shape verification

- [ ] Key membership checked.
- [ ] JSON-category/type checks cover new structures.
- [ ] Existing documented residual limits remain explicit.

### LLM-102 — Extend command registration verifier

- [ ] Every new local-LLM Tauri command is registered.
- [ ] Frontend-invoked names are checked against Rust registration.
- [ ] Negative rename probe fails as expected.

---

## P11 — Frontend and Rust Test Coverage

### LLM-110 — Rust settings/provider tests

- [ ] migration;
- [ ] defaults;
- [ ] provider routing;
- [ ] missing key fail-closed;
- [ ] missing local model fail-closed;
- [ ] no Fake fallback;
- [ ] provider/model switching ordering.

### LLM-111 — Catalog/installer tests

- [ ] all P3/P4 adversarial cases automated.

### LLM-112 — Runtime manager tests

- [ ] lazy load;
- [ ] reuse;
- [ ] unload;
- [ ] model switch;
- [ ] delete coordination;
- [ ] concurrent generation serialization;
- [ ] shutdown/cancellation.

### LLM-113 — Local text caller integration tests

- [ ] ambient local generation;
- [ ] typed local generation;
- [ ] memory Off/On behavior;
- [ ] transcript save Off/On behavior;
- [ ] muted behavior;
- [ ] TTS error recovery;
- [ ] local model error recovery.

### LLM-114 — Frontend settings tests

- [ ] Local/Google selection;
- [ ] model status rendering;
- [ ] download progress;
- [ ] cancel;
- [ ] checksum/download failure;
- [ ] delete;
- [ ] local test result;
- [ ] Google controls conditional behavior;
- [ ] voice-cloud disclosure remains visible/accurate.

---

## P12 — Real CPU Model Acceptance

### LLM-120 — Build explicit real-model acceptance harness

- [ ] Disabled by default in ordinary unit CI.
- [ ] Accepts catalog model ID/revision.
- [ ] Downloads through production installer or uses a verified cached artifact.
- [ ] Runs CPU-only.
- [ ] Runs with generation network denied after install.
- [ ] Produces machine-readable report.

### LLM-121 — Accept SmolLM2-360M on CPU

Record:

- [ ] exact model ID/revision/hash;
- [ ] model bytes;
- [ ] host CPU/OS;
- [ ] cold load time;
- [ ] memory/RSS delta or best available measurement;
- [ ] first-token latency if available;
- [ ] tokens/sec;
- [ ] 60-token ambient-style generation latency;
- [ ] unload/reload success.

### LLM-122 — Smoke second catalog model

- [ ] Qwen3-0.6B (or selected second model) loads and generates on CPU.
- [ ] Non-thinking behavior verified if applicable.
- [ ] Record same basic identity/performance evidence.

### LLM-123 — Define recommended model from evidence

- [ ] Confirm SmolLM2 remains recommended default or change recommendation based on measured usability.
- [ ] Record rationale without rewriting provider architecture.

---

## P13 — Packaging and CI

### LLM-130 — Make native runtime compile on all supported CI targets

- [ ] macOS arm64.
- [ ] macOS x86_64.
- [ ] Linux CI test host if used for Rust quality.
- [ ] No developer-local system llama.cpp dependency required.

### LLM-131 — Keep ordinary CI model-weight-free

- [ ] `npm run check:all` does not download GGUF weights.
- [ ] Unit tests use fake runtime injection.
- [ ] Generated-contract exporter does not load/download a model.
- [ ] Bundle jobs do not download model weights.

### LLM-132 — Add dependency/license inventory coverage

- [ ] llama.cpp/runtime binding represented in license collector.
- [ ] Any native shipped dependencies have evidence.
- [ ] Model catalog licenses documented separately from shipped code dependencies.

### LLM-133 — Record bundle-size impact

- [ ] Compare pre/post native app bundle size.
- [ ] Confirm GGUF weights are not embedded.

---

## P14 — Documentation

### LLM-140 — Update README

- [ ] Gemini API key no longer described as universally required.
- [ ] Explain Local text provider quick start.
- [ ] Explain model download size and CPU expectations.
- [ ] Explain voice remains Gemini Live in this phase.

### LLM-141 — Add local LLM architecture document

- [ ] Provider selection.
- [ ] Runtime manager.
- [ ] catalog/installer.
- [ ] privacy/network boundaries.
- [ ] future fully local voice seam.

### LLM-142 — Add local model catalog maintenance guide

- [ ] How to choose supported artifacts.
- [ ] How to pin revisions.
- [ ] How to verify SHA/bytes.
- [ ] How to add license metadata.
- [ ] How to run real-model acceptance.

### LLM-143 — Update privacy documentation

- [ ] Local text-generation semantics.
- [ ] Model-download network behavior.
- [ ] Distinguish local generation from Google TTS/Gemini Live.

### LLM-144 — Update AGENTS/developer gates

- [ ] Document any new local-LLM check scripts.
- [ ] Document real-model acceptance separately from ordinary CI.

---

## P15 — Final Reconciliation

### LLM-150 — Run canonical quality gate

- [ ] clean `npm ci`;
- [ ] `npm run check:all`;
- [ ] generated-tree guard;
- [ ] generated backend contract drift/shape gate;
- [ ] frontend command registration verifier;
- [ ] dependency audit;
- [ ] supported macOS bundle jobs.

### LLM-151 — Run local-provider negative probes

- [ ] Google missing key fails; does not Fake/local fallback.
- [ ] Local missing model fails; does not Google/Fake fallback.
- [ ] Corrupt checksum fails installation.
- [ ] Corrupt GGUF fails load safely.
- [ ] Network-denied local generation succeeds through real/injected runtime as appropriate.
- [ ] Model switch/delete race does not corrupt runtime state.

### LLM-152 — Re-audit privacy and silent-failure behavior

- [ ] No prompt/output logging.
- [ ] No provider substitution.
- [ ] No swallowed install/generation errors that leave UI reporting success.
- [ ] No stale optimistic local-model state after backend rejection.
- [ ] No preview-only fake behavior reachable in production.

### LLM-153 — Produce final reconciliation report

Create:

`docs/RECONCILIATION_LOCAL_LLM_<date>.md`

Record per task:

- [ ] implementation evidence;
- [ ] tests;
- [ ] environment;
- [ ] CI run IDs;
- [ ] real-model artifact identities;
- [ ] CPU performance evidence;
- [ ] known residual limitations;
- [ ] deferred fully-local-voice work.

---

# Final Gate — Local LLM V1

Do not mark Local LLM V1 complete until all are true:

- [ ] Explicit Local/Google text provider selection exists.
- [ ] Production provider fallback to Fake is eliminated.
- [ ] Local provider never silently falls back to Gemini.
- [ ] SmolLM2-360M-Instruct Q4_K_M is pinned, installable, verified, and CPU-tested.
- [ ] A second catalog model proves model choice is not hard-coded to one GGUF.
- [ ] Ambient remarks work with Local.
- [ ] Typed messages work with Local.
- [ ] Local generation performs no network I/O after model installation.
- [ ] Existing memory/transcript/privacy/speech state semantics remain intact.
- [ ] Model installer is integrity-checked, cancellable, atomic, and path-safe.
- [ ] Model switch/delete is race-safe.
- [ ] Settings migration preserves existing Google users.
- [ ] Settings UI accurately distinguishes local text from cloud voice/TTS.
- [ ] Generated contract and IPC integrity gates cover the new surface.
- [ ] Ordinary CI remains independent of third-party GGUF downloads.
- [ ] CPU real-model acceptance evidence is recorded.
- [ ] Runtime/model licensing is reconciled.
- [ ] `npm run check:all` and release/static/bundle gates are green.
- [ ] Final reconciliation explicitly defers `Moonshine -> Local LLM -> TTS` voice conversation to a later phase.
