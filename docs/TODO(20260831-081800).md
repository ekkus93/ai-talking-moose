# AI Talking Moose — Local LLM Provider TODO

**Companion spec:** `docs/SPEC(20260831-081800).md`
**Scope:** Local CPU-friendly text-generation provider alongside Gemini; fully local voice is deferred.

Task IDs use the `LLM-###` prefix.

**Status (2026-09-03):** P0-P15 are implemented and reconciled, and the **Local LLM V1 Final Gate is complete**. Final evidence is recorded in `docs/RECONCILIATION_LOCAL_LLM_20260903.md`. Exact merged implementation `master` SHA `9b3e8621fb41836365f0d5af2167564025279956` passed CI run `33736402906`; PR #43 stage-1 reconciliation head `9338823063e1101a9abaf0155cbbc4e071f2f94d` passed CI run `33741103743`, including job `100606705422` executing the literal canonical `npm run check:all` after clean `npm ci`. The final docs-closeout head must remain green before merge.

---

## P0 — Architecture and Baseline Reconciliation

### LLM-001 — Freeze the local-LLM V1 boundary

- [x] Record the current authoritative `master` SHA before implementation.
- [x] Confirm Local LLM V1 covers ambient remarks and typed `send_text_message` generation only.
- [x] Confirm Gemini Live remains the voice-session provider in this phase.
- [x] Confirm Moonshine local ASR behavior is unchanged by this phase.
- [x] Confirm fully local voice is explicitly deferred.

**Acceptance**

- [x] Developer docs contain one unambiguous diagram for Local Text V1 and one for the deferred local-voice path.
- [x] No implementation task requires `LocalTextModel` to implement `RealtimeConversationProvider`.

### LLM-002 — Audit every current use of the generic AI `provider` setting

- [x] Enumerate Rust reads/writes.
- [x] Enumerate TypeScript reads/writes.
- [x] Enumerate generated-contract fields/fixtures.
- [x] Enumerate tests depending on `provider = "fake"`.
- [x] Classify Fake provider usage as test-only, browser-preview-only, or production-visible.
- [x] Identify every current Google-without-key path that manufactures Fake behavior.

**Acceptance**

- [x] Audit is recorded in implementation notes/reconciliation.
- [x] No production fallback is left unidentified.

### LLM-003 — Choose and document the llama.cpp integration mechanism

- [x] Evaluate maintained Rust bindings versus a bundled subprocess design.
- [x] Require supported macOS arm64/x86_64 builds.
- [x] Require CPU-only operation.
- [x] Require normal packaged-app operation without a separately installed llama.cpp executable.
- [x] Record native dependency/license implications.
- [x] Record cancellation/thread-safety constraints of the chosen API.

**Acceptance**

- [x] One mechanism is selected with rationale.
- [x] A minimal compile-only proof builds in CI before deeper runtime work begins.

---

## P1 — Settings Schema and Migration

### LLM-010 — Introduce a typed text-provider setting

- [x] Add Rust `TextProvider` enum or equivalent authoritative typed representation.
- [x] Support at least `google` and `local`.
- [x] Do not expose `fake` as a normal production provider choice.
- [x] Add corresponding TypeScript type.
- [x] Add generated-contract representative shape/value coverage.

### LLM-011 — Split provider-specific text model selections

- [x] Replace ambiguous `text_model` ownership with `google_text_model` and `local_text_model` or an equivalent unambiguous schema.
- [x] Preserve `live_model` and `tts_model` independently.
- [x] Keep local model ID stable and app-owned rather than using arbitrary filenames as persisted identity.

### LLM-012 — Bump settings version and migrate existing profiles

- [x] Existing `text_model` migrates to `google_text_model`.
- [x] Existing profiles migrate to `text_provider = google`.
- [x] Initialize `local_text_model` to the recommended local catalog ID without marking it installed.
- [x] Preserve all unrelated settings.
- [x] Migration is idempotent.
- [x] Normalized settings are persisted once.

**Acceptance**

- [x] Legacy-current fixture migrates deterministically.
- [x] Already-migrated fixture does not report another migration.
- [x] Existing Google model choice survives exactly.
- [x] Generated frontend contract and TypeScript interface agree.

### LLM-013 — Decide and encode the new-profile default

- [x] Prefer Local text generation for new profiles unless implementation evidence gives a strong reason not to.
- [x] If Local is default, selecting the model MUST NOT automatically download it.
- [x] Onboarding/settings must explain the required model install.
- [x] If Google remains default, record rationale explicitly. *(N/A: Local is the chosen new-profile default.)*

---

## P2 — Remove Unsafe Provider Fallbacks

### LLM-020 — Make `AppState::get_text_model()` fail explicit rather than manufacture Fake success

- [x] Remove production Google-without-key -> `FakeTextModel` behavior.
- [x] Select Google only when `text_provider = google`.
- [x] Select Local only when `text_provider = local`.
- [x] Preserve explicit fake injection for unit tests/browser-preview infrastructure only.
- [x] Ensure missing Google credential returns safe auth/setup failure.
- [x] Ensure missing local model returns safe model/setup failure.

**Acceptance**

- [x] Test proves Google without credential never calls FakeTextModel.
- [x] Test proves Local missing model never calls Google.
- [x] Test proves local runtime failure never calls Fake or Google.

### LLM-021 — Audit Fake provider production reachability

- [x] Prove packaged/native production cannot select Fake through persisted ordinary settings.
- [x] Keep browser preview explicit and development-scoped if Fake behavior remains there.
- [x] Add regression test for production reachability boundary.

---

## P3 — Local Model Catalog

### LLM-030 — Define authoritative local model descriptor types

Include at minimum:

- [x] stable model ID;
- [x] display name;
- [x] family;
- [x] parameter scale;
- [x] quantization;
- [x] artifact filename;
- [x] source URL;
- [x] pinned revision;
- [x] expected bytes;
- [x] SHA-256;
- [x] license metadata;
- [x] context limit;
- [x] recommended max output;
- [x] template/runtime hints if required.

### LLM-031 — Add SmolLM2-360M-Instruct Q4_K_M catalog entry

- [x] Verify authoritative artifact source.
- [x] Pin immutable revision.
- [x] Verify exact filename.
- [x] Verify expected byte count.
- [x] Compute/verify SHA-256 independently.
- [x] Record license and notice requirements.
- [x] Record supported chat-template behavior in chosen llama.cpp runtime.

**Do not close this task from guessed metadata.**

### LLM-032 — Add a second supported catalog entry

Target Qwen3-0.6B Q4_K_M or the closest current compatible instruct/chat artifact.

- [x] Pin/verify artifact metadata as above.
- [x] Confirm non-thinking behavior/template configuration.
- [x] Confirm CPU load/generation works with the chosen runtime.

### LLM-033 — Add catalog validation tests

- [x] IDs unique.
- [x] Filenames path-safe.
- [x] SHA values syntactically valid.
- [x] Expected bytes nonzero/reasonable.
- [x] Revisions nonempty and pinned.
- [x] Default selected local ID exists.
- [x] License/source metadata present.

---

## P4 — Local Model Storage and Installer

### LLM-040 — Create local LLM model-root policy

- [x] Derive an app-managed LLM model root from application data location.
- [x] Use a layout scoped by model ID and revision.
- [x] Keep test roots temporary and isolated.
- [x] Prevent path traversal from catalog fields.

### LLM-041 — Implement install-state model

- [x] `not_installed` or equivalent.
- [x] `downloading`.
- [x] `verifying`.
- [x] `installed`.
- [x] `failed` with safe error category/message.
- [x] Define behavior after restart with stale partial files.

### LLM-042 — Implement verified download

- [x] Explicit user action required.
- [x] HTTPS-only source.
- [x] Temporary/staging file.
- [x] Bounded declared size.
- [x] Progress events.
- [x] Cancellation.
- [x] SHA-256 verification.
- [x] Expected-size verification.
- [x] Atomic final install.
- [x] Cleanup on failure/cancel.

### LLM-043 — Add adversarial installer tests

- [x] wrong SHA;
- [x] wrong byte count;
- [x] truncated response;
- [x] oversized response;
- [x] interrupted/cancelled download;
- [x] stale temp file;
- [x] malicious/path-traversal catalog filename;
- [x] atomic rename/install failure;
- [x] duplicate concurrent install attempt.

### LLM-044 — Implement model deletion

- [x] Delete installed artifact and model-owned directory only.
- [x] Coordinate with loaded/active inference.
- [x] Unload before deletion.
- [x] Define selected-but-deleted UI/settings behavior.
- [x] Never silently select another model.

### LLM-045 — Expose model lifecycle commands through Tauri

Likely commands or equivalent:

- [x] list local LLM models/status;
- [x] install model;
- [x] cancel install;
- [x] delete model;
- [x] get local LLM diagnostics/status;
- [x] test selected local model.

- [x] Add all command names to IPC registration-contract verification.
- [x] Add frontend fixtures and production-path tests.

---

## P5 — llama.cpp Runtime Integration

### LLM-050 — Add isolated local runtime module

- [x] Create `src-tauri/src/ai/local/` or equivalent focused module tree.
- [x] Keep binding-specific types out of `AppState`, command modules, and frontend-facing types.
- [x] Add runtime manager ownership/lifecycle.

### LLM-051 — Implement model load/unload

- [x] Lazy load selected model on first generation/test.
- [x] Verify install state before opening artifact.
- [x] Configure CPU execution.
- [x] Configure bounded context.
- [x] Configure conservative thread count.
- [x] Release model/context memory on unload.
- [x] Sanitize runtime errors.

### LLM-052 — Implement shared runtime synchronization

- [x] Serialize generation unless chosen runtime is proven safe for concurrency.
- [x] Prevent delete/switch while the runtime is using the artifact.
- [x] Prevent use-after-unload races.
- [x] Define application-shutdown behavior.

### LLM-053 — Implement request cancellation/bounds

- [x] Bound output tokens.
- [x] Bound context/prompt size according to explicit policy.
- [x] Provide cancellation/interrupt if runtime API supports it.
- [x] Ensure app shutdown cannot hang indefinitely on inference.

### LLM-054 — Add safe runtime diagnostics

Record without prompt/output text:

- [x] selected model ID;
- [x] revision/quantization;
- [x] loaded state;
- [x] thread count;
- [x] context size;
- [x] last load/generation error category;
- [x] last generation duration;
- [x] token counts/tokens-per-second if available.

---

## P6 — `LocalTextModel`

### LLM-060 — Implement `TextModel` for local inference

- [x] Accept `TextRequest` unchanged from callers.
- [x] Apply system instruction correctly.
- [x] Apply user prompt correctly.
- [x] Apply temperature when supported.
- [x] Apply bounded max output.
- [x] Return provider-neutral `TextResponse`.
- [x] Map failures to safe `ProviderError` values.

### LLM-061 — Implement chat-template policy

- [x] Prefer model/runtime-provided chat template.
- [x] Do not hand-concatenate raw control tokens at ambient/chat call sites.
- [x] Fail explicitly when template/runtime compatibility is missing.
- [x] Add fixture tests for each supported model family.

### LLM-062 — Disable thinking/reasoning mode for supported models where applicable

- [x] Configure Qwen-family non-thinking behavior if used.
- [x] Ensure hidden reasoning is not returned in `TextResponse`.
- [x] Ensure hidden reasoning is not logged/persisted/spoken.

### LLM-063 — Define local text output policy

- [x] Ambient remains ~60 requested tokens and 320 delivered characters.
- [x] Typed local responses use a concise local-specific output cap (~128–192 target unless testing changes it).
- [x] Gemini output policy remains independently configurable/current.
- [x] Empty local output is treated as failure/empty result according to explicit policy, never manufactured success.

---

## P7 — Provider Routing

### LLM-070 — Route ambient remarks through selected text provider

- [x] No caller-specific provider branching; continue using `TextModel` abstraction.
- [x] Preserve both privacy gates.
- [x] Preserve character-state transitions.
- [x] Preserve output bounds.
- [x] Preserve existing TTS/speech controller.

### LLM-071 — Route typed `send_text_message` through selected text provider

- [x] Preserve message input bound.
- [x] Preserve memory privacy.
- [x] Preserve system prompt.
- [x] Preserve transcript-retention policy.
- [x] Preserve muted behavior.
- [x] Preserve speech synthesis path.
- [x] Restore Idle correctly on local failure.

### LLM-072 — Prove Gemini Live voice remains independent

- [x] `start_conversation()` behavior unchanged for Local text selection.
- [x] `get_live_provider()` does not inspect Local text selection to choose Fake/local.
- [x] Moonshine finalized transcripts continue to use the existing Live provider in this phase.
- [x] Add regression test documenting this boundary.

---

## P8 — Frontend AI Settings UX

### LLM-080 — Refactor Gemini-only AI tab into provider-aware sections

- [x] Add Text Generation provider selector: Local / Google Gemini.
- [x] Separate Text Generation controls from Voice Conversation controls.
- [x] Avoid presenting Gemini API key as mandatory when Local text is selected.

### LLM-081 — Add local model selector and status

For each model show:

- [x] display name;
- [x] approximate download size;
- [x] installed/not-installed state;
- [x] recommended/smallest quality hint;
- [x] selected state;
- [x] download/install progress;
- [x] safe failure state.

### LLM-082 — Add install/cancel/delete actions

- [x] Download button for uninstalled selected model.
- [x] Progress display.
- [x] Cancel during download where supported.
- [x] Delete installed model.
- [x] Confirm destructive delete if consistent with existing UX conventions.
- [x] Do not silently switch selection on delete.

### LLM-083 — Add “Test Local Model” action

- [x] Requires installed selected model.
- [x] Runs short bounded prompt.
- [x] Works with network denied after installation.
- [x] Displays safe success/failure result.
- [x] Does not leak generated test content into logs.

### LLM-084 — Preserve Google controls

- [x] API-key save/remove remains secure-store-backed.
- [x] Gemini text-model selector uses `google_text_model`.
- [x] Gemini Live model selector remains available for voice.
- [x] Existing Google connection test remains Google-specific.

### LLM-085 — Add cloud/local disclosure text

- [x] Clearly state Local affects text generation in this phase.
- [x] Clearly state Gemini Live voice is still cloud-based.
- [x] Clearly state Google TTS may still receive locally generated text if selected.
- [x] Keep privacy text synchronized with actual settings.

---

## P9 — Privacy, Security, and Fail-Closed Behavior

### LLM-090 — Prove no network I/O during local generation

- [x] Run local TextModel path under repository network-denial harness.
- [x] Verify generation succeeds through injected/test runtime without network.
- [x] Verify no Google request builder/client is reached.
- [x] Model download is tested separately as explicit network behavior.

### LLM-091 — Add local-LLM log privacy tests

Prove logs do not contain:

- [x] typed prompt sentinel;
- [x] system prompt sentinel;
- [x] memory sentinel;
- [x] ambient summary sentinel;
- [x] model output sentinel.

- [x] Keep capture tests non-vacuous using established liveness harness.

### LLM-092 — Sanitize local runtime failures

- [x] Raw runtime/binding errors stay internal unless demonstrably safe.
- [x] Frontend receives stable safe categories/messages.
- [x] Paths and prompt text are excluded from user-visible errors.

### LLM-093 — Harden artifact paths

- [x] Catalog IDs cannot traverse directories.
- [x] Filenames cannot be absolute or escape root.
- [x] Symlink behavior is explicitly safe during install/delete.
- [x] Delete cannot remove parent/unrelated files.

### LLM-094 — Audit model license/download policy

- [x] SmolLM2 entry license verified.
- [x] Qwen entry license verified.
- [x] Runtime/binding license included in shipped dependency inventory.
- [x] Any required model notices documented.

---

## P10 — Generated Contract and IPC Integrity

### LLM-100 — Extend backend contract exporter

- [x] New settings fields emitted.
- [x] New local model descriptor/status/diagnostics shapes emitted.
- [x] Representative instances cover newly added IPC-crossing types.
- [x] JSON remains owned by Rust exporter and passes exact drift gate.

### LLM-101 — Extend TypeScript shape verification

- [x] Key membership checked.
- [x] JSON-category/type checks cover new structures.
- [x] Existing documented residual limits remain explicit.

### LLM-102 — Extend command registration verifier

- [x] Every new local-LLM Tauri command is registered.
- [x] Frontend-invoked names are checked against Rust registration.
- [x] Negative rename probe fails as expected.

---

## P11 — Frontend and Rust Test Coverage

### LLM-110 — Rust settings/provider tests

- [x] migration;
- [x] defaults;
- [x] provider routing;
- [x] missing key fail-closed;
- [x] missing local model fail-closed;
- [x] no Fake fallback;
- [x] provider/model switching ordering.

### LLM-111 — Catalog/installer tests

- [x] all P3/P4 adversarial cases automated.

### LLM-112 — Runtime manager tests

- [x] lazy load;
- [x] reuse;
- [x] unload;
- [x] model switch;
- [x] delete coordination;
- [x] concurrent generation serialization;
- [x] shutdown/cancellation.

### LLM-113 — Local text caller integration tests

- [x] ambient local generation;
- [x] typed local generation;
- [x] memory Off/On behavior;
- [x] transcript save Off/On behavior;
- [x] muted behavior;
- [x] TTS error recovery;
- [x] local model error recovery.

### LLM-114 — Frontend settings tests

- [x] Local/Google selection;
- [x] model status rendering;
- [x] download progress;
- [x] cancel;
- [x] checksum/download failure;
- [x] delete;
- [x] local test result;
- [x] Google controls conditional behavior;
- [x] voice-cloud disclosure remains visible/accurate.

---

## P12 — Real CPU Model Acceptance

### LLM-120 — Build explicit real-model acceptance harness

- [x] Disabled by default in ordinary unit CI.
- [x] Accepts catalog model ID/revision.
- [x] Downloads through production installer or uses a verified cached artifact.
- [x] Runs CPU-only.
- [x] Runs with generation network denied after install.
- [x] Produces machine-readable report.

### LLM-121 — Accept SmolLM2-360M on CPU

Record:

- [x] exact model ID/revision/hash;
- [x] model bytes;
- [x] host CPU/OS;
- [x] cold load time;
- [x] memory/RSS delta or best available measurement;
- [x] first-token latency if available;
- [x] tokens/sec;
- [x] 60-token ambient-style generation latency;
- [x] unload/reload success.

### LLM-122 — Smoke second catalog model

- [x] Qwen3-0.6B (or selected second model) loads and generates on CPU.
- [x] Non-thinking behavior verified if applicable.
- [x] Record same basic identity/performance evidence.

### LLM-123 — Define recommended model from evidence

- [x] Confirm SmolLM2 remains recommended default or change recommendation based on measured usability.
- [x] Record rationale without rewriting provider architecture.

---

## P13 — Packaging and CI

### LLM-130 — Make native runtime compile on all supported CI targets

- [x] macOS arm64.
- [x] macOS x86_64.
- [x] Linux CI test host if used for Rust quality.
- [x] No developer-local system llama.cpp dependency required.

### LLM-131 — Keep ordinary CI model-weight-free

- [x] `npm run check:all` does not download GGUF weights.
- [x] Unit tests use fake runtime injection.
- [x] Generated-contract exporter does not load/download a model.
- [x] Bundle jobs do not download model weights.

### LLM-132 — Add dependency/license inventory coverage

- [x] llama.cpp/runtime binding represented in license collector.
- [x] Any native shipped dependencies have evidence.
- [x] Model catalog licenses documented separately from shipped code dependencies.

### LLM-133 — Record bundle-size impact

- [x] Compare pre/post native app bundle size.
- [x] Confirm GGUF weights are not embedded.

---

## P14 — Documentation and Developer Experience

### LLM-140 — Update README

- [x] Gemini API key no longer described as universally required.
- [x] Explain Local text provider quick start.
- [x] Explain model download size and CPU expectations.
- [x] Explain voice remains Gemini Live in this phase.

### LLM-141 — Add local LLM architecture document

- [x] Provider selection.
- [x] Runtime manager.
- [x] catalog/installer.
- [x] privacy/network boundaries.
- [x] future fully local voice seam.

### LLM-142 — Add local model catalog maintenance guide

- [x] How to choose supported artifacts.
- [x] How to pin revisions.
- [x] How to verify SHA/bytes.
- [x] How to add license metadata.
- [x] How to run real-model acceptance.

### LLM-143 — Update privacy documentation

- [x] Local text-generation semantics.
- [x] Model-download network behavior.
- [x] Distinguish local generation from Google TTS/Gemini Live.

### LLM-144 — Update AGENTS/developer gates

- [x] Document any new local-LLM check scripts.
- [x] Document real-model acceptance separately from ordinary CI.

---

## P15 — Final Reconciliation

### LLM-150 — Run canonical quality gate

- [x] clean `npm ci`;
- [x] `npm run check:all`;
- [x] generated-tree guard;
- [x] generated backend contract drift/shape gate;
- [x] frontend command registration verifier;
- [x] dependency audit;
- [x] supported macOS bundle jobs.

**Evidence:** exact implementation-master CI `33736402906`; final-reconciliation CI `33741103743`; canonical aggregate job `100606705422` on exact head `9338823063e1101a9abaf0155cbbc4e071f2f94d`.

### LLM-151 — Run local-provider negative probes

- [x] Google missing key fails; does not Fake/local fallback.
- [x] Local missing model fails; does not Google/Fake fallback.
- [x] Corrupt checksum fails installation.
- [x] Corrupt GGUF fails load safely.
- [x] Network-denied local generation succeeds through real/injected runtime as appropriate.
- [x] Model switch/delete race does not corrupt runtime state.

**Evidence:** PR #41 CI `33728972880`, subsequent exact-master Rust suites, and real-GGUF network-denied acceptance `33663595759`; detailed test mapping is in `docs/RECONCILIATION_LOCAL_LLM_20260903.md`.

### LLM-152 — Re-audit privacy and silent-failure behavior

- [x] No prompt/output logging.
- [x] No provider substitution.
- [x] No swallowed install/generation errors that leave UI reporting success.
- [x] No stale optimistic local-model state after backend rejection.
- [x] No preview-only fake behavior reachable in production.

**Evidence:** final P15 audit plus PR #42 persisted-settings fail-closed remediation; exact PR #42 CI `33734840062`; exact merged implementation-master CI `33736402906`.

### LLM-153 — Produce final reconciliation report

Create:

`docs/RECONCILIATION_LOCAL_LLM_<date>.md`

Record per task:

- [x] implementation evidence;
- [x] tests;
- [x] environment;
- [x] CI run IDs;
- [x] real-model artifact identities;
- [x] CPU performance evidence;
- [x] known residual limitations;
- [x] deferred fully-local-voice work.

**Final report:** `docs/RECONCILIATION_LOCAL_LLM_20260903.md`.

---

# Final Gate — Local LLM V1

Do not mark Local LLM V1 complete until all are true:

- [x] Explicit Local/Google text provider selection exists.
- [x] Production provider fallback to Fake is eliminated.
- [x] Local provider never silently falls back to Gemini.
- [x] SmolLM2-360M-Instruct Q4_K_M is pinned, installable, verified, and CPU-tested.
- [x] A second catalog model proves model choice is not hard-coded to one GGUF.
- [x] Ambient remarks work with Local.
- [x] Typed messages work with Local.
- [x] Local generation performs no network I/O after model installation.
- [x] Existing memory/transcript/privacy/speech state semantics remain intact.
- [x] Model installer is integrity-checked, cancellable, atomic, and path-safe.
- [x] Model switch/delete is race-safe.
- [x] Settings migration preserves existing Google users.
- [x] Settings UI accurately distinguishes local text from cloud voice/TTS.
- [x] Generated contract and IPC integrity gates cover the new surface.
- [x] Ordinary CI remains independent of third-party GGUF downloads.
- [x] CPU real-model acceptance evidence is recorded.
- [x] Runtime/model licensing is reconciled.
- [x] `npm run check:all` and release/static/bundle gates are green.
- [x] Final reconciliation explicitly defers `Moonshine -> Local LLM -> TTS` voice conversation to a later phase.

**Local LLM V1 Final Gate: COMPLETE.** See `docs/RECONCILIATION_LOCAL_LLM_20260903.md` for canonical evidence, model identities/performance, packaging/licensing data, residual limitations, and the explicit fully-local-voice deferral.