# Local LLM TODO

## P0 — Architecture and Baseline

### LLM-001 — Document the text-vs-voice boundary

- [ ] Document Local text generation as independent from Gemini Live voice conversation.
- [ ] Document that selecting Local text does not make voice conversation local.
- [ ] Document cloud TTS implications when Local text is spoken through Google TTS.

### LLM-002 — Audit provider selection and fallback behavior

- [ ] Enumerate every production text-provider selection path.
- [ ] Enumerate every production voice-provider selection path.
- [ ] Identify and remove production Fake/synthetic success fallback paths where they can hide configuration/runtime failure.
- [ ] Document any intentionally retained test-only Fake providers.

### LLM-003 — Select and prove llama.cpp binding

- [ ] Pin exact Rust binding version.
- [ ] Pin exact sys crate version if separate.
- [ ] Prove compile on supported macOS arm64.
- [ ] Prove compile on supported macOS x86_64.
- [ ] Prove compile on Linux CI target.
- [ ] Document CPU-only runtime configuration.
- [ ] Document thread-safety/Send/Sync constraints of model/context/sampler types.
- [ ] Document cancellation/abort capabilities and limitations.

---

## P1 — Settings and Schema

### LLM-010 — Introduce explicit text provider

- [ ] Add `TextProvider::{Google, Local}` or equivalent.
- [ ] Keep live voice provider independently modeled.
- [ ] Add serialization/deserialization tests.

### LLM-011 — Split text model settings

- [ ] Add `google_text_model`.
- [ ] Add `local_text_model`.
- [ ] Remove ambiguous shared text-model field where applicable.
- [ ] Add defaults for both.

### LLM-012 — Migrate existing settings safely

- [ ] Existing users preserve Google behavior.
- [ ] Old model field migrates into Google text model.
- [ ] Local provider is not selected implicitly for existing profiles.
- [ ] Add migration regression tests.

### LLM-013 — Decide new-profile default after real-model acceptance

- [ ] Keep Google default until Local acceptance data exists.
- [ ] After P12, explicitly decide whether new profiles default to Local or Google.
- [ ] Record decision and evidence.

---

## P2 — Provider Safety / Fail-Closed Semantics

### LLM-020 — Remove unsafe Fake production fallback

- [ ] Missing Google key returns safe provider error.
- [ ] Missing Local model returns safe provider error.
- [ ] No production path manufactures success text.
- [ ] No Local failure silently substitutes Google.
- [ ] No Google failure silently substitutes Local.
- [ ] Add negative regression tests.

### LLM-021 — Keep test fakes isolated

- [ ] Test-only fake providers remain behind test-only construction/path.
- [ ] Production `AppState` does not select Fake provider from ordinary settings.

---

## P3 — Local Model Catalog

### LLM-030 — Define catalog schema

Each entry records:

- [ ] stable ID;
- [ ] display name;
- [ ] family;
- [ ] revision/commit;
- [ ] exact artifact filename;
- [ ] exact URL;
- [ ] SHA-256;
- [ ] expected bytes;
- [ ] quantization;
- [ ] context limit;
- [ ] recommended max output;
- [ ] license metadata;
- [ ] template/runtime hints.

### LLM-031 — Pin SmolLM2 candidate

- [ ] Exact SmolLM2-360M-Instruct revision.
- [ ] Exact GGUF artifact.
- [ ] Exact SHA-256.
- [ ] Exact bytes.
- [ ] License evidence.
- [ ] Context/output bounds.

### LLM-032 — Pin Qwen candidate

- [ ] Exact Qwen3-0.6B revision.
- [ ] Exact GGUF artifact.
- [ ] Exact SHA-256.
- [ ] Exact bytes.
- [ ] License evidence.
- [ ] Context/output bounds.
- [ ] Non-thinking policy hint.

### LLM-033 — Validate catalog

- [ ] Unique IDs.
- [ ] Unique artifact names.
- [ ] HTTPS-only URLs.
- [ ] SHA format validation.
- [ ] Size bounds.
- [ ] Safe path fields.
- [ ] Model IDs cannot traverse filesystem.
- [ ] Artifact filenames cannot traverse filesystem.

---

## P4 — Model Installer and Storage

### LLM-040 — Define model storage root

- [ ] Use application-private model root.
- [ ] Keep test roots temporary and isolated.
- [ ] Prevent path traversal from catalog fields.

### LLM-041 — Implement install-state model

- [ ] `not_installed` or equivalent.
- [ ] `downloading`.
- [ ] `verifying`.
- [ ] `installed`.
- [ ] `failed` with safe error category/message.
- [ ] Define behavior after restart with stale partial files.

### LLM-042 — Implement verified download

- [ ] Explicit user action required.
- [ ] HTTPS-only source.
- [ ] Temporary/staging file.
- [ ] Bounded declared size.
- [ ] Progress events.
- [ ] Cancellation.
- [ ] SHA-256 verification.
- [ ] Expected-size verification.
- [ ] Atomic final install.
- [ ] Cleanup on failure/cancel.

### LLM-043 — Add adversarial installer tests

- [ ] wrong SHA;
- [ ] wrong byte count;
- [ ] truncated response;
- [ ] oversized response;
- [ ] interrupted/cancelled download;
- [ ] stale temp file;
- [ ] malicious/path-traversal catalog filename;
- [ ] atomic rename/install failure;
- [ ] duplicate concurrent install attempt.

### LLM-044 — Implement model deletion

- [ ] Delete installed artifact and model-owned directory only.
- [ ] Coordinate with loaded/active inference.
- [ ] Unload before deletion.
- [ ] Define selected-but-deleted UI/settings behavior.
- [ ] Never silently select another model.

### LLM-045 — Expose model lifecycle commands through Tauri

Likely commands or equivalent:

- [ ] list local LLM models/status;
- [ ] install model;
- [ ] cancel install;
- [ ] delete model;
- [ ] get local LLM diagnostics/status;
- [ ] test selected local model.

- [ ] Add all command names to IPC registration-contract verification.
- [ ] Add frontend fixtures and production-path tests.

---

## P5 — llama.cpp Runtime Integration

### LLM-050 — Add isolated local runtime module

- [x] Create `src-tauri/src/ai/local/` or equivalent focused module tree.
- [x] Keep binding-specific types out of `AppState`, command modules, and frontend-facing types.
- [x] Add runtime manager ownership/lifecycle.

### LLM-051 — Implement model load/unload

- [x] Lazy load selected model on first generation/test.
- [x] Verify install state before opening artifact.
- [x] Configure CPU execution.
- [x] Configure bounded context.
- [x] Configure conservative thread count.
- [x] Release model/context memory on unload.
- [x] Sanitize runtime errors.

### LLM-052 — Implement shared runtime synchronization

- [x] Serialize generation unless chosen runtime is proven safe for concurrency.
- [x] Prevent delete/switch while the runtime is using the artifact.
- [x] Prevent use-after-unload races.
- [x] Define application-shutdown behavior.

### LLM-053 — Implement request cancellation/bounds

- [x] Bound output tokens.
- [x] Bound context/prompt size according to explicit policy.
- [x] Provide cancellation/interrupt if runtime API supports it.
- [x] Ensure app shutdown cannot hang indefinitely on inference.

### LLM-054 — Add safe runtime diagnostics

Record without prompt/output text:

- [x] selected model ID;
- [x] revision/quantization;
- [x] loaded state;
- [x] thread count;
- [x] context size;
- [x] last load/generation error category;
- [x] last generation duration;
- [x] token counts/tokens-per-second if available.

---

## P6 — `LocalTextModel`

### LLM-060 — Implement `TextModel` for local inference

- [x] Accept `TextRequest` unchanged from callers.
- [x] Apply system instruction correctly.
- [x] Apply user prompt correctly.
- [x] Apply temperature when supported.
- [x] Apply bounded max output.
- [x] Return provider-neutral `TextResponse`.
- [x] Map failures to safe `ProviderError` values.

### LLM-061 — Implement chat-template policy

- [ ] Prefer model/runtime-provided chat template.
- [ ] Do not hand-concatenate raw control tokens at ambient/chat call sites.
- [ ] Fail explicitly when template/runtime compatibility is missing.
- [ ] Add fixture tests for each supported model family.

### LLM-062 — Disable thinking/reasoning mode for supported models where applicable

- [ ] Configure Qwen-family non-thinking behavior if used.
- [ ] Ensure hidden reasoning is not returned in `TextResponse`.
- [ ] Ensure hidden reasoning is not logged/persisted/spoken.

### LLM-063 — Define local text output policy

- [ ] Ambient remains ~60 requested tokens and 320 delivered characters.
- [ ] Typed local responses use a concise local-specific output cap (~128–192 target unless testing changes it).
- [ ] Gemini output policy remains independently configurable/current.
- [ ] Empty local output is treated as failure/empty result according to explicit policy, never manufactured success.

---

## P7 — Provider Routing

### LLM-070 — Route ambient remarks through selected text provider

- [ ] No caller-specific provider branching; continue using `TextModel` abstraction.
- [ ] Preserve both privacy gates.
- [ ] Preserve character-state transitions.
- [ ] Preserve output bounds.
- [ ] Preserve existing TTS/speech controller.

### LLM-071 — Route typed `send_text_message` through selected text provider

- [ ] Preserve message input bound.
- [ ] Preserve memory privacy.
- [ ] Preserve system prompt.
- [ ] Preserve transcript-retention policy.
- [ ] Preserve muted behavior.
- [ ] Preserve speech synthesis path.
- [ ] Restore Idle correctly on local failure.

### LLM-072 — Keep Gemini Live voice independent

- [ ] Local text selection does not alter `start_conversation()`.
- [ ] Local text selection does not change `get_live_provider()`.
- [ ] Moonshine-to-Gemini-Live voice remains unchanged.

---

## P8 — Frontend Local LLM UI

### LLM-080 — Add provider-aware AI settings

- [ ] Local / Google Gemini text selector.
- [ ] Separate text-generation controls from voice-conversation controls.
- [ ] Do not imply Google key is required for Local text.

### LLM-081 — Add local model chooser

Show:

- [ ] name;
- [ ] size;
- [ ] installed state;
- [ ] recommendation;
- [ ] selected model;
- [ ] download progress;
- [ ] safe errors.

### LLM-082 — Add install lifecycle UI

- [ ] Download.
- [ ] Progress.
- [ ] Cancel.
- [ ] Delete.
- [ ] Optional confirmation.
- [ ] Never silently change selection after deletion.

### LLM-083 — Add `Test Local Model`

- [ ] Require selected model installed.
- [ ] Use tiny bounded test prompt.
- [ ] Deny network during test generation.
- [ ] Return safe success/failure.
- [ ] Do not log generated text.

### LLM-084 — Preserve Google controls

- [ ] Google key management still works.
- [ ] Google text model selection still works.
- [ ] Voice and TTS controls remain independent.

### LLM-085 — Add accurate privacy disclosure

- [ ] Explain Local applies to text generation.
- [ ] Explain Gemini Live voice remains cloud.
- [ ] Explain Google TTS may receive locally generated text when enabled.

---

## P9 — Security, Privacy, and Fail-Closed Validation

### LLM-090 — Prove zero network I/O during Local generation

- [ ] Run local generation with network disabled after installation.
- [ ] Add harness/report evidence.

### LLM-091 — Add log-sentinel privacy tests

Prove logs exclude:

- [ ] typed prompt;
- [ ] system prompt;
- [ ] memories;
- [ ] ambient summaries;
- [ ] generated output.

### LLM-092 — Harden runtime failure messages

- [ ] No raw llama.cpp messages reach UI.
- [ ] Stable safe categories.
- [ ] No model path in user-facing failure.
- [ ] No prompt fragments in failure.

### LLM-093 — Complete artifact-path hardening

- [ ] No model-ID traversal.
- [ ] No artifact-filename traversal.
- [ ] Symlink behavior explicitly safe.
- [ ] Delete cannot escape model directory.

### LLM-094 — Complete license audit

- [ ] SmolLM2 notice/license evidence.
- [ ] Qwen notice/license evidence.
- [ ] llama.cpp/binding dependency evidence.
- [ ] Packaged notices included as required.

---

## P10 — Generated Contracts / IPC

### LLM-100 — Extend backend contract

Include:

- [ ] new settings fields;
- [ ] local model descriptor/status objects;
- [ ] runtime diagnostics;
- [ ] representative Local LLM objects.

### LLM-101 — Extend TypeScript shape verifier

- [ ] Validate all new Local LLM contract fields.
- [ ] Add negative drift probes.

### LLM-102 — Extend command registration verifier

- [ ] Cover every Local LLM command.
- [ ] Include eventual test command.
- [ ] Add negative rename probe.

---

## P11 — Test Coverage

### LLM-110 — Settings/provider tests

- [ ] migration;
- [ ] defaults;
- [ ] provider routing;
- [ ] missing credentials/model;
- [ ] no fallback;
- [ ] model switching.

### LLM-111 — Installer tests

- [ ] Ensure all P3/P4 adversarial installer cases are automated.

### LLM-112 — Runtime tests

- [ ] lazy load;
- [ ] reuse;
- [ ] unload;
- [ ] switch;
- [ ] deletion;
- [ ] serialization;
- [ ] shutdown/cancellation.

### LLM-113 — Caller integration tests

- [ ] Local ambient.
- [ ] Local typed text.
- [ ] Memory on/off.
- [ ] Transcript retention.
- [ ] Muted mode.
- [ ] TTS failure.
- [ ] Local model failure.

### LLM-114 — Frontend tests

- [ ] provider selection;
- [ ] install lifecycle;
- [ ] safe errors;
- [ ] test Local model;
- [ ] Google control independence;
- [ ] privacy disclosure.

---

## P12 — Real CPU Model Acceptance

### LLM-120 — Build separate real-model harness

- [ ] Disabled in normal CI.
- [ ] Records model/revision.
- [ ] Uses production installer or verified cache.
- [ ] Forces CPU.
- [ ] Disables network during generation.
- [ ] Emits machine-readable report.

### LLM-121 — Run SmolLM2 acceptance

Record:

- [ ] exact hash/revision/bytes;
- [ ] CPU/OS;
- [ ] cold-load time;
- [ ] RAM impact;
- [ ] first-token latency if obtainable;
- [ ] tokens/sec;
- [ ] 60-token Moose generation latency;
- [ ] unload/reload behavior.

### LLM-122 — Run Qwen3 acceptance

- [ ] Basic generation works.
- [ ] Non-thinking behavior proven.
- [ ] Same core metrics captured.

### LLM-123 — Decide default model

- [ ] Compare SmolLM2 and Qwen3 measurements.
- [ ] Record default-model decision.
- [ ] Revisit LLM-013 new-profile provider default.

---

## P13 — Packaging / CI

### LLM-130 — Reconcile native build proof

- [ ] macOS arm64.
- [ ] macOS x86_64.
- [ ] Linux CI.
- [ ] No system llama.cpp requirement.
- [ ] Ordinary CI does not download GGUF weights.
- [ ] Contract generation does not load weights.
- [ ] Application bundles do not contain GGUF weights.
- [ ] Dependency/license collector includes native runtime dependencies.
- [ ] Record bundle-size impact.

---

## P14 — Documentation

### LLM-140 — Update README

- [ ] Local text quick-start.
- [ ] Model size/CPU expectations.
- [ ] Explain voice remains cloud.

### LLM-141 — Add Local LLM architecture document

- [ ] Provider abstraction.
- [ ] Installer.
- [ ] runtime.
- [ ] caller routing.
- [ ] failure semantics.

### LLM-142 — Add catalog maintenance guide

- [ ] Pinning revisions.
- [ ] Updating SHA/bytes.
- [ ] License evidence.
- [ ] Real-model acceptance steps.

### LLM-143 — Update privacy documentation

- [ ] Local text data path.
- [ ] Gemini Live cloud path.
- [ ] TTS cloud path.

### LLM-144 — Update developer/AGENTS guidance

- [ ] Required validation gates.
- [ ] How to run real-model harness.
- [ ] No silent provider fallback rule.

---

## P15 — Final Reconciliation

### LLM-150 — Run canonical complete quality suite

- [ ] clean `npm ci`;
- [ ] `npm run check:all`;
- [ ] generated-tree checks;
- [ ] contract checks;
- [ ] IPC registration;
- [ ] dependency audit;
- [ ] both macOS bundles.

### LLM-151 — Run negative probes

- [ ] missing Google key;
- [ ] missing Local model;
- [ ] corrupt checksum;
- [ ] corrupt GGUF;
- [ ] network-denied Local generation;
- [ ] switch/delete races.

### LLM-152 — Final silent-failure/privacy audit

- [ ] no prompt/output logging;
- [ ] no provider substitution;
- [ ] no swallowed failures;
- [ ] no stale optimistic UI;
- [ ] no Fake production path.

### LLM-153 — Create reconciliation record

- [ ] Create `docs/RECONCILIATION_LOCAL_LLM_<date>.md`.
- [ ] Record implementation evidence.
- [ ] Record test/CI evidence.
- [ ] Record model/performance evidence.
- [ ] Record known limitations.
