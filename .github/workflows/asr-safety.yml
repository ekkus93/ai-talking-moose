name: ASR Rust Safety

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - "src-tauri/src/asr/moonshine/ffi.rs"
      - "src-tauri/src/asr/moonshine/manifest.rs"
      - "src-tauri/src/asr/moonshine/runtime.rs"
      - "tools/asr-miri/**"
      - ".github/workflows/asr-safety.yml"
  pull_request:
    branches:
      - master
    paths:
      - "src-tauri/src/asr/moonshine/ffi.rs"
      - "src-tauri/src/asr/moonshine/manifest.rs"
      - "src-tauri/src/asr/moonshine/runtime.rs"
      - "tools/asr-miri/**"
      - ".github/workflows/asr-safety.yml"

permissions:
  contents: read

concurrency:
  group: asr-rust-safety-${{ github.ref }}
  cancel-in-progress: true

jobs:
  miri:
    name: Moonshine fake-ABI ownership under Miri
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install nightly Miri
        run: rustup toolchain install nightly --profile minimal --component miri

      - name: Prepare Miri sysroot
        run: cargo +nightly miri setup

      - name: Run production Moonshine wrapper tests under Miri
        run: cargo +nightly miri test --manifest-path tools/asr-miri/Cargo.toml
